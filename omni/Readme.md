# Omni On Prem

Followed these instructions to the letter

[Run Omni On-Prem](https://docs.siderolabs.com/omni/self-hosted/run-omni-on-prem

Some small differences can be seen in the `compose.yaml` and in the `.env`. These are supposed to be generated by the `populate_scripts.bash` script but they were created with errors so I fixed them manually.
