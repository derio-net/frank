apiVersion: v1
kind: Service
metadata:
  name: www
  namespace: www-system
spec:
  selector:
    app.kubernetes.io/name: www
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
