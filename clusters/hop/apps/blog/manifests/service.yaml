apiVersion: v1
kind: Service
metadata:
  name: blog
  namespace: blog-system
spec:
  selector:
    app.kubernetes.io/name: blog
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
