apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: headscale-system

resources:
  - backup-cronjob.yaml
  - configmap.yaml
  - deployment.yaml
  - key-expiry-cronjob.yaml
  - pvc.yaml
  - service.yaml
  - tailscale-client.yaml

configMapGenerator:
  - name: headscale-api-key-expiry-script
    files:
      - files/key-expiry-check.py
