apiVersion: v1
kind: Service
metadata:
  name: landing
  namespace: landing-system
spec:
  selector:
    app.kubernetes.io/name: landing
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
