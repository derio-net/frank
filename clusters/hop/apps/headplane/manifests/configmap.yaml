apiVersion: v1
kind: ConfigMap
metadata:
  name: headplane-config
  namespace: headscale-system
data:
  config.yaml: |
    server:
      host: "0.0.0.0"
      port: 3000
      cookie_secret: "hop-headplane-cookie-secret-32c!"
      cookie_secure: false

    headscale:
      url: "http://headscale.headscale-system.svc:8080"

