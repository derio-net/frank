apiVersion: v1
kind: Service
metadata:
  name: headplane
  namespace: headscale-system
spec:
  selector:
    app.kubernetes.io/name: headplane
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
