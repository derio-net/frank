# Acceptance matrix — the registry of business-level acceptance tests and
# where each is verified. Rendered by `fr acceptance report`; checked locally
# with `fr acceptance check`.
#
# Row schema:
#   id:         kebab-case, stable
#   capability: grouping (tables render in first-seen order)
#   acceptance: the business-level statement
#   origin:     list of "repo:path[#anchor]" refs (spec §, design doc)
#   levels:     unit/api/int/ui → list of "repo:path[#Lline]" test refs
#               ([] = level does not verify this row)
#   status:     ci | scheduled | skipped | not-implemented | failing
#     ci               automated on every PR — cannot drift silently
#     scheduled        automated on cron/path triggers
#     skipped          verification exists but does not run in CI (proven
#                      live once / manual walk) → CI warning, backfill owed
#     not-implemented  no test or surface exists yet → CI warning
#     failing          known red → `fr acceptance check` exits 2, CI FAILS
#   notes:      evidence detail, drift context
#
# Rule: update rows in the SAME PR that changes a Test Plan, adds tests, ships
# a surface, or touches CI.
# Add rows with `fr acceptance add` (schema-validated append). Keep `rows:`
# as the LAST top-level key — `add` appends to the end of this file.

org: derio-net
repo: frank
rows:
  - id: gpu1-usb-25g-replacement-documented
    capability: gpu-1 network stability
    acceptance: Operators have a documented, MAC-bound path to move gpu-1 from the damaged
      onboard Realtek NIC to the replacement USB 2.5G Ethernet adapter without changing node identity.
    origin:
    - frank:docs/superpowers/specs/2026-07-09--gpu--gpu-1-usb-25g-nic-design.md
    levels: {}
    status: not-implemented
    notes: Repo-prep row; verified by plan phase 1 docs/template validation.
  - id: gpu1-usb-25g-node-ip-stable
    capability: gpu-1 network stability
    acceptance: gpu-1 keeps serving the same Kubernetes node identity and 192.168.55.31
      address through the replacement USB 2.5G Ethernet adapter.
    origin:
    - frank:docs/superpowers/specs/2026-07-09--gpu--gpu-1-usb-25g-nic-design.md
    levels: {}
    status: skipped
    notes: Live manual proof 2026-07-11 — ConfigPatch 404-gpu1-usb-25g-nic applied;
      gpu-1 Ready on 192.168.55.31 via enp0s20f0u7 MAC 6c:1f:f7:c6:e0:da.
  - id: gpu1-usb-25g-detected
    capability: gpu-1 network stability
    acceptance: Talos detects the USB 2.5G Ethernet adapter with a usable driver, stable link state, and discoverable MAC address before the concrete network patch is applied.
    origin:
    - frank:docs/superpowers/specs/2026-07-09--gpu--gpu-1-usb-25g-nic-design.md
    levels: {}
    status: skipped
    notes: Live manual proof 2026-07-11 — Talos detected r8152 USB adapter enp0s20f0u7
      with MAC 6c:1f:f7:c6:e0:da; missing rtl8156b firmware patch logged but link works.
  - id: gpu1-usb-25g-firmware-present
    capability: gpu-1 network stability
    acceptance: gpu-1's Talos schematic ships the RTL8156B firmware (rtl_nic/rtl8156b-2.fw)
      for the USB 2.5G Ethernet adapter, so the r8152 driver loads it cleanly with no
      "Direct firmware load ... failed" warning at boot.
    origin:
    - frank:docs/superpowers/specs/2026-07-09--gpu--gpu-1-usb-25g-nic-design.md
    levels: {}
    status: skipped
    notes: 'Live manual proof 2026-07-16 — siderolabs/realtek-firmware (v20260309) applied to
      402-gpu1-nvidia-extensions.yaml via omnictl; Omni recomputed schematic 54e23848, reinstalled
      + rebooted gpu-1 (~76s NotReady). Post-boot: extension present, ZERO rtl_nic firmware-load
      failures in dmesg (both rtl8156b USB and rtl8125b onboard cleared), gpu-1 Ready on
      192.168.55.31 (USB NIC re-enumerated enp0s20f0u7->enp0s20f0u5, MAC-bound patch kept the
      address), nvidia allocatable 1, 19/19 pods back, cilium datapath OK.'
  - id: gpu1-cilium-datapath-stable
    capability: gpu-1 network stability
    acceptance: Cilium on gpu-1 initializes and keeps a healthy direct-routing datapath
      on the replacement NIC, with no layer-1 NIC flap alert breach during the soak.
    origin:
    - frank:docs/superpowers/specs/2026-07-09--gpu--gpu-1-usb-25g-nic-design.md
    levels: {}
    status: not-implemented
    notes: Manual post-swap Cilium verification plus 24h carrier-change soak.
  - id: gpu1-gpu-workloads-return
    capability: gpu-1 workload continuity
    acceptance: After the NIC maintenance event, gpu-1 returns with NVIDIA allocatable
      GPU capacity and hard-pinned GPU/agent workloads recover to their intended state.
    origin:
    - frank:docs/superpowers/specs/2026-07-09--gpu--gpu-1-usb-25g-nic-design.md
    levels: {}
    status: skipped
    notes: Live manual proof 2026-07-11 — gpu-1 allocatable nvidia.com/gpu is 1;
      pinned workloads are Running or Completed after the network move.
  - id: net-all-hosts-declarative-dns
    capability: Network isolation
    acceptance: Every Frank host has the internal DNS resolvers (192.168.10.11/12) set
      declaratively in its Talos machine config, so no host silently depends on DHCP-provided
      DNS.
    origin:
    - frank:docs/superpowers/implemented/specs/2026-07-12--net--declarative-nameservers-design.md
    levels:
      unit:
      - frank:scripts/tests/test_cluster_wide_nameservers.py
    status: skipped
    notes: 'Live proof 2026-07-12: 102-cluster-nameservers applied via omnictl to all
      7 Frank nodes (non-disruptive); talosctl get resolvers on gpu-1/mini-1/2/3/pc-1/raspi-1/2
      all show 192.168.10.11 + 192.168.10.12 from the config layer. Unit guard
      scripts/tests/test_cluster_wide_nameservers.py passing (local guard, not CI-run).'
  - id: net-static-host-resolves-internal-dns
    capability: Network isolation
    acceptance: A static-networked Frank host (e.g. gpu-1 on its USB adapter) resolves
      DNS via the internal resolvers and boots cleanly, with no fallback to ACL-blocked
      public DNS.
    origin:
    - frank:docs/superpowers/implemented/specs/2026-07-12--net--declarative-nameservers-design.md
    levels: {}
    status: skipped
    notes: 'Live proof 2026-07-12: gpu-1 (static USB-adapter host) rebooted via talosctl
      reboot — DOWN 16:51 -> Ready 16:53 (~2min, no time-sync boot-hang); resolvers
      192.168.10.11/12 from the config layer; siderolink reconnected; GPU workloads
      rescheduled Running. The exact incident scenario now boots clean.'
  - id: net-gpu1-single-declarative-ip
    capability: Network isolation
    acceptance: gpu-1 presents a single declarative IPv4 address on its USB adapter
      (no DHCP-drift 192.168.55.150 alongside the static 192.168.55.31).
    origin:
    - frank:docs/superpowers/implemented/specs/2026-07-12--net--declarative-nameservers-design.md
    levels: {}
    status: skipped
    notes: 'Live proof 2026-07-12: after console-drift cleanup, talosctl -n 192.168.55.31
      get addresses shows a single 192.168.55.31/24 on enp0s20f0u7 (no .150), and it
      persisted across the gpu-1 reboot-survival test.'
  - id: hermes-dashboard-api-routable
    capability: Hermes dashboard exposure
    acceptance: The Hermes web dashboard (hermes.cluster.derio.net) and gateway API
      (hermes-api.cluster.derio.net) are reachable through a Traefik IngressRoute gated
      by ip-allowlist + security-headers, with user authentication provided by the dashboard's
      own basic-auth (frank#626) rather than authentik forward-auth.
    origin:
    - frank:apps/traefik/manifests/ingressroutes.yaml
    - frank:docs/superpowers/implemented/specs/2026-06-03--orch--hermes-agent-shell-deploy-design.md
    levels: {}
    status: not-implemented
    notes: '2026-07-12 (willikins#285): authentik forward-auth removed from both hermes
      routes — no authentik application/proxy-provider was ever configured for these
      hosts, so the outpost 404''d every request. Mechanism is now the dashboard''s
      own basic-auth (HERMES_DASHBOARD_BASIC_AUTH_*, frank#626; self-auth proven internally
      :9119/login -> 200). In-repo surface exists; becomes live-provable once this PR
      merges and ArgoCD reconciles the traefik-system IngressRoutes. SSO via authentik
      is a possible later follow-up.'
  - id: obs-alert-triage-classifies
    capability: Frank alert triage
    acceptance: The operator can invoke the frank-alert-triage skill and receive a per-alert
      verdict table classifying every firing Grafana alert (muted-canary, by-design,
      false-positive, known-tracker, or unexplained) without mutating the cluster.
    origin:
    - frank:docs/superpowers/implemented/specs/2026-07-13--obs--alert-triage-and-agent-report-fixes-design.md
    levels: {}
    status: skipped
    notes: >-
      Live proof 2026-07-13: ran the frank-alert-triage skill against the live firing
      set — 2 cert-canaries muted, Layer 11 by-design (frank-ops#11), zero mutation;
      verdict table matched the manual triage. classify.py unit-tested across 8
      decision-tree branches. github_issue is implemented as an orthogonal tracker
      annotation, not a benign known-tracker verdict, so a live NotReady pod still
      escalates.
  - id: obs-alert-agent-evidence-not-hn
    capability: alert-agent report quality
    acceptance: The alert-agent attributes a traffic surge from evidence (top referrers/user-agents)
      or states undetermined; it never defaults to a named source such as Hacker News.
    origin:
    - frank:docs/superpowers/implemented/specs/2026-07-13--obs--alert-triage-and-agent-report-fixes-design.md
    levels: {}
    status: skipped
    notes: >-
      Live proof 2026-07-13: the Hacker News anchor was removed from SKILL.md and the
      orchestration.py surge/digest prompts (verified live in the rolled pod); a live
      surge reply attributed the traffic to a Chrome/120 stale-UA crawler from
      evidence, with no Hacker News. Prompt-content unit tests enforce the absence.
      (#631, #633)
  - id: obs-alert-agent-plain-text-table
    capability: alert-agent report quality
    acceptance: Operator DM and digest replies from the alert-agent are compact plain-text
      tables, never a raw JSON envelope or a prose wall.
    origin:
    - frank:docs/superpowers/implemented/specs/2026-07-13--obs--alert-triage-and-agent-report-fixes-design.md
    levels: {}
    status: skipped
    notes: >-
      Live proof 2026-07-13: drove session_send then render_payload end-to-end in the
      deployed pod — the agent's rich dict result renders as a compact plain-text
      table (aligned as_of/gpu_mode/alerts_firing/edge_traffic_level rows),
      raw-JSON=false. Fixed mechanically in render_payload (#634) after instruction-only
      attempts (#631 removed then #633 restored the text envelope) proved unable to
      override the session server's per-turn raw-JSON wrapper. Bridge unit tests cover
      the table render.
  - id: obs-alert-agent-answers-explicit-asks
    capability: alert-agent report quality
    acceptance: A focused explicit DM question to the alert-agent returns a real answer
      within the turn budget, not a timeout fallback.
    origin:
    - frank:docs/superpowers/implemented/specs/2026-07-13--obs--alert-triage-and-agent-report-fixes-design.md
    levels: {}
    status: skipped
    notes: >-
      Live proof 2026-07-13: a focused DM turn returned status=ok within the bounded
      DM_TIMEOUT_S=150 (no timeout fallback). An overrun is mechanically backstopped by
      a deterministic frank-facts surge-compute snapshot, and a transport error can no
      longer silently drop the reply (#633). Bridge unit tests cover both the fallback
      and the exception path.
  - id: cnc-staging-secrets-in-vcluster
    capability: CNC staging secret delivery
    acceptance: The three CNC staging secrets (cnc-secrets, cnc-ghcr-pull, cnc-runner-auth)
      are present inside the cnc-staging vCluster with their expected names and keys,
      so cncd/node/fru can mount them.
    origin:
    - frank:docs/superpowers/specs/2026-07-19--secrets--cnc-staging-vcluster-secret-sync-design.md
    levels: {}
    status: skipped
    notes: 'Live manual proof 2026-07-19 — all 3 secrets present inside the cnc-staging
      vCluster ns cnc-staging with correct keys (cnc-secrets/CNCD_SECRETS_MASTER_KEY,
      cnc-runner-auth/token, cnc-ghcr-pull/.dockerconfigjson) via fromHost.secrets
      after operator populated Infisical (prod env) + forced-sync/restart. No CI
      (host ESO + live vCluster required).'
  - id: cnc-staging-ghcr-pull-dockerconfigjson
    capability: CNC staging secret delivery
    acceptance: The cnc-ghcr-pull secret synced into the cnc-staging vCluster retains
      type kubernetes.io/dockerconfigjson, so CNC image pulls from ghcr succeed.
    origin:
    - frank:docs/superpowers/specs/2026-07-19--secrets--cnc-staging-vcluster-secret-sync-design.md
    levels: {}
    status: skipped
    notes: 'Live manual proof 2026-07-19 — cnc-ghcr-pull synced into the cnc-staging
      vCluster (ns cnc-staging) RETAINED type kubernetes.io/dockerconfigjson (the
      primary risk); key .dockerconfigjson present. Verified via the vCluster
      kubeconfig. No CI (live vCluster required).'
  - id: cnc-prod-secret-delivery-unaffected
    capability: CNC staging secret delivery
    acceptance: CNC prod secret delivery (ExternalSecrets resolved directly in host
      namespace cnc) is unchanged by the staging secret-sync work.
    origin:
    - frank:docs/superpowers/specs/2026-07-19--secrets--cnc-staging-vcluster-secret-sync-design.md
    levels: {}
    status: skipped
    notes: 'Statically guarded by scripts/tests/test_cnc_staging_host_secrets.py::test_prod_overlay_still_includes_the_three_externalsecrets
      (prod overlay still renders all 3 ExternalSecrets; cnc-base byte-unchanged —
      the staging change is a staging-scoped $patch:delete). Local guard (frank does
      not CI-run scripts/tests/); prod runtime path untouched by this PR.'
  - id: stoa-mirror-all-private
    capability: stoa CI on Frank
    acceptance: Every private agentic-stoa repo has a Gitea mirror on Frank whose main
      branch tracks GitHub automatically.
    origin:
    - frank:docs/superpowers/specs/2026-07-19--cicd--stoa-full-mirror-gitea-actions-design.md
    levels: {}
    status: skipped
    notes: "Live proof 2026-07-20 \u2014 all 10 private repos on Gitea; second-brain/hermes-brain/flexible-health AND the cnc trio (discovered absent, never provisioned since June) created + sha-verified backfilled via scripts/tmp/phase5-gitea-actions.sh."
  - id: stoa-ci-runs-on-frank
    capability: stoa CI on Frank
    acceptance: The Actions workflows of the five CI-bearing agentic-stoa repos execute
      on Frank via Gitea Actions, on PR sync branches, main pushes, and their schedules.
    origin:
    - frank:docs/superpowers/specs/2026-07-19--cicd--stoa-full-mirror-gitea-actions-design.md
    levels: {}
    status: skipped
    notes: "Live proof 2026-07-20 \u2014 smoke PR second-brain#12 sync-pr-12; tests/unittest SUCCESS on act-runner; 14+ tasks executed across second-brain/hermes-brain/cnc-frd. Schedule-tick observation still pending."
  - id: stoa-ci-github-status-writeback
    capability: stoa CI on Frank
    acceptance: A Frank-side CI run reports its result as a commit status on the originating
      GitHub PR sha, keeping branch protection meaningful.
    origin:
    - frank:docs/superpowers/specs/2026-07-19--cicd--stoa-full-mirror-gitea-actions-design.md
    levels: {}
    status: skipped
    notes: "Live proof 2026-07-20 \u2014 gitea-actions/* statuses on GitHub sha 24f4291c (pending then success/failure transitions) via stoa-status-bridge, 6 PipelineRuns Succeeded. Required frank#664 (EL apply freeze) first."
  - id: stoa-ci-no-double-mutation
    capability: stoa CI on Frank
    acceptance: While GitHub Actions and Frank CI run in parallel, mutating jobs (PR
      robots, tags, releases, issue upserts) execute from exactly one operator-selectable
      authority side.
    origin:
    - frank:docs/superpowers/specs/2026-07-19--cicd--stoa-full-mirror-gitea-actions-design.md
    levels: {}
    status: not-implemented
    notes: "Guards merged in all 5 repos + CI_AUTHORITY=github org variable live (phase5 script). Stays open until the negative is observed \u2014 first Gitea-side pins-update tick skipping cleanly and no duplicate robot PRs during the August parallel week."
  - id: report-tables-list-of-dicts
    capability: alert-agent report formatting
    acceptance: A list-of-uniform-dicts payload renders as an aligned monospace column
      table in Telegram, not escaped one-line JSON.
    origin:
    - frank:docs/superpowers/specs/2026-07-22--obs--telegram-report-tables-design.md
    levels:
      unit:
      - frank:apps/alert-agent/telegram-bridge/tests/test_bridge.py
    status: skipped
    notes: Unit-proven by render_report tests (aligned table, ragged-key union columns,
      first-seen order) over the captured live /edge_traffic fixture. Local guard —
      frank does not CI-run apps/*/tests; live-observed post-merge (report-tables-live-observed).
  - id: report-tables-no-silent-truncation
    capability: alert-agent report formatting
    acceptance: Over-cap lists show a +N more footer; no value is ever cut mid-character
      or mid-JSON.
    origin:
    - frank:docs/superpowers/specs/2026-07-22--obs--telegram-report-tables-design.md
    levels:
      unit:
      - frank:apps/alert-agent/telegram-bridge/tests/test_bridge.py
    status: skipped
    notes: Unit-proven — 11-row fixture renders 10 rows + "+1 more", each shown value
      whole; scalar-list entries appear whole (the 200-char json-truncation defect is
      gone). Local guard (no CI run); live-observed post-merge.
  - id: report-tables-html-escape-safe
    capability: alert-agent report formatting
    acceptance: Values containing angle brackets or ampersands deliver (never a silent
      HTML-400), proven by unit test and the 400-to-plain fallback path.
    origin:
    - frank:docs/superpowers/specs/2026-07-22--obs--telegram-report-tables-design.md
    levels:
      unit:
      - frank:apps/alert-agent/telegram-bridge/tests/test_bridge.py
    status: skipped
    notes: Unit-proven — every value html.escaped (only <pre> tags are literal angle
      brackets); send_reply retries once as plain text on a canned 400, data preserved.
      Local guard (no CI run); the escape path is exercised end-to-end live post-merge.
  - id: report-tables-live-observed
    capability: alert-agent report formatting
    acceptance: A live command turn in the deployed pod renders a real monospace table
      (operator-observed, post-merge).
    origin:
    - frank:docs/superpowers/specs/2026-07-22--obs--telegram-report-tables-design.md
    levels: {}
    status: skipped
    notes: Live proof 2026-07-22 (PR 668 merged, ArgoCD synced fb6e41c, pod rolled onto
      bridge-cm with render_report/send_reply/_split_for_telegram present). Drove a
      realistic /edge_traffic payload through the DEPLOYED render_payload then send_reply,
      producing aligned monospace column tables, a ragged COUNTRY column with blank cells,
      a value with angle-brackets and ampersand HTML-escaped, sendMessage parse_mode=HTML. Fed the
      exact deployed code path (the agent turn itself hit the pre-existing DM latency
      ceiling, out of scope here). Local guard — no CI run.
  - id: cred-expiry-warns-before-expiry
    capability: alert-agent credential expiry
    acceptance: When the refresh token is 7 or fewer days from expiry, the daily check
      sends a Telegram warning whose urgency escalates at 3, 1, and expired.
    origin:
    - frank:docs/superpowers/specs/2026-07-22--obs--cred-expiry-alert-design.md
    levels:
      unit:
      - frank:apps/alert-agent/handlers/tests/test_cred_expiry.py
    status: skipped
    notes: Unit-proven — evaluate_expiry tier boundaries (8→ok, 7→notice, 3→soon,
      1→urgent, 0/-5→expired), should_warn iff tier!=ok, escalating plain-text wording;
      run_cred_check sends the warning via tg_send on threshold. Local guard (frank does
      not CI-run apps/*/tests); delivery observed live post-merge (cred-expiry-live-observed).
  - id: cred-expiry-heartbeat-deadman
    capability: alert-agent credential expiry
    acceptance: A Grafana rule pages Telegram directly if the daily heartbeat line stops
      appearing in VictoriaLogs (checker died or pod down).
    origin:
    - frank:docs/superpowers/specs/2026-07-22--obs--cred-expiry-alert-design.md
    levels:
      unit:
      - frank:scripts/tests/test_cred_expiry_alert_rule.py
    status: skipped
    notes: Rule shape unit-pinned — VictoriaLogs stats query on the _msg field (NOT Hop's
      log field; verified live), lt 1 + noDataState OK, telegram_direct, 30h window,
      for 2h. Query validated server-side against live VictoriaLogs (returns 0 pre-deploy).
      Local guard; end-to-end firing observed live post-merge.
  - id: cred-expiry-robust-on-bad-cred
    capability: alert-agent credential expiry
    acceptance: A missing, unparseable, or field-less credential file yields a warning
      plus an error-tier heartbeat, never a silent skip.
    origin:
    - frank:docs/superpowers/specs/2026-07-22--obs--cred-expiry-alert-design.md
    levels:
      unit:
      - frank:apps/alert-agent/handlers/tests/test_cred_expiry.py
    status: skipped
    notes: Unit-proven — missing/unparseable/field-less/bool/Infinity/NaN/out-of-range-epoch
      + non-FileNotFound read + invalid-UTF-8 all yield tier=error + a warning + a heartbeat;
      evaluate_expiry is total (never raises) and run_cred_check always heartbeats even on
      an unexpected error. Local guard; no CI run.
  - id: cred-expiry-live-observed
    capability: alert-agent credential expiry
    acceptance: Post-merge, both the warning and the dead-man heartbeat signals are
      triggered and observed live.
    origin:
    - frank:docs/superpowers/specs/2026-07-22--obs--cred-expiry-alert-design.md
    levels: {}
    status: skipped
    notes: Live proof 2026-07-22 (after PR 671 plus the PR 673 nested-field fix rolled).
      Heartbeat run against the REAL cred gave days_left=27 tier=ok; seeded to VictoriaLogs
      (dead-man query value=1). Forced 2-days-out cred via CRED_PATH delivered a real Telegram
      warning (tier=urgent). Grafana rule alert-agent-cred-expiry-heartbeat-stale loaded
      (provisioning API) and evaluates state=inactive/health=ok against the live heartbeat.
      Live-only.
  - id: agent-shell-talosctl-cluster-skew
    capability: Agent shells
    acceptance: Operators driving Talos from an agent shell use a talosctl whose version
      is supported against the running cluster, instead of one three minor versions
      behind it.
    origin:
    - frank:docs/superpowers/specs/2026-07-23--agents--agent-images-upstream-version-sweep-design.md
    levels: {}
    status: skipped
    notes: 'VERIFIED LIVE 2026-07-23 — secure-agent-pod kali container (image
      secure-agent-kali:c7a80f6) reports talosctl client Tag: v1.12.6, matching the
      cluster (all 7 nodes Talos v1.12.6). The three-minor skew (was v1.9.5, outside
      Talos''s supported +/-1) is closed. The audit script''s cluster anchor also
      resolves to v1.12.6 and marks the pin ok. Live manual proof; no CI level.'
  - id: agent-shell-sweep-boots-clean
    capability: Agent shells
    acceptance: Every agent shell Frank deploys still boots and serves after the upstream
      version sweep rebuilds it.
    origin:
    - frank:docs/superpowers/specs/2026-07-23--agents--agent-images-upstream-version-sweep-design.md
    levels: {}
    status: skipped
    notes: 'VERIFIED LIVE 2026-07-23 — all rebuilt shells Running on c7a80f6: alert-agent
      (3 multi-agent-shell containers, s6-rc started clean, no crashloop-bail), n8n-01
      (multi-agent-shell sidecar), secure-agent-pod (kali + vk-local 2/2), paperclip
      (paperclip-shell 2/2), ruflo (ruflo-server + ruflo-shell 2/2), hermes-agent-shell
      (hermes + hindsight + ssh 3/3). cnc-base NOT covered: its runner StatefulSet is
      not currently deployed (cnc-prod app OutOfSync/Missing, pre-existing — the
      manual-login persistent agent is down, unrelated to the sweep). Live manual proof.'
  - id: hermes-upgrade-reaches-pod
    capability: Agent shells
    acceptance: A Hermes version bump actually reaches the running shell, rather than
      shipping in the image while the pod keeps serving the old venv off its PVC.
    origin:
    - frank:docs/superpowers/specs/2026-07-23--agents--agent-images-upstream-version-sweep-design.md
    levels: {}
    status: skipped
    notes: 'VERIFIED LIVE 2026-07-23, with a topology correction the live check surfaced.
      The DEPLOYED consumer of the bumped image is the hermes-agent-shell-ssh sidecar
      (built FROM docker v2026.7.20): its running container reports "Hermes Agent v0.19.0
      (2026.7.20)" — the bump reached the pod. TWO nuances the spec''s image->app mapping
      got wrong: (1) the base hermes-agent-shell image (PyPI 0.19.0, the .seed-version
      PVC-venv one this row was written about) is NOT deployed anywhere in frank — same
      status as infra-shell — so its bump is validated by CI smoke only; (2) the pod''s
      runtime "hermes" container is docker.io/nousresearch/hermes-agent:v2026.7.7.2 (=0.18.2),
      pinned DIRECTLY in the manifest, NOT a ghcr.io/derio-net/* image, so the sweep did
      not touch it. Consequence: the intent_ack_continuation manual-op (frank plan
      P2.T2.S7) is moot for the current deployment — see the plan note. Live manual proof.'
  - id: ruflo-revendor-preserves-function
    capability: Ruflo
    acceptance: Ruflo still serves its UI and handles file upload and download after
      the upstream re-vendor, proving the local GridFS and wasm-scheme patches survived.
    origin:
    - frank:docs/superpowers/specs/2026-07-23--agents--agent-images-upstream-version-sweep-design.md
    levels: {}
    status: skipped
    notes: 'VERIFIED LIVE 2026-07-23 — ruflo Running on ruflo-server:c7a80f6, readiness
      1/1, /api/v2/feature-flags returns valid JSON. Both local patches confirmed present
      in the RUNNING build: the GridFS shim (Readable.from / new Writable) compiled into
      /app/build/server/chunks/database-*.js, and the wasm allow-line in urlSafety-*.js.
      The re-vendor (a6dd4ab->26c35b5) preserved both. package.json version 0.20.0.
      Live manual proof (endpoint + compiled-patch presence); a full authenticated file
      round-trip was not exercised, but the patch code is demonstrably in the artifact.'
  - id: agent-images-pin-drift-measurable
    capability: Agent image maintenance
    acceptance: An operator can re-measure how far every agent-images pin has drifted
      from upstream on demand, without redoing the manual registry-by-registry reconnaissance.
    origin:
    - frank:docs/superpowers/specs/2026-07-23--agents--agent-images-upstream-version-sweep-design.md
    levels: {}
    status: skipped
    notes: 'VERIFIED LIVE 2026-07-23 — scripts/version_audit.py (agent-images main c7a80f6)
      run against the live cluster reports every bumped pin ok, and its talosctl anchor
      resolves to the cluster''s v1.12.6 (not GitHub latest v1.13.7), reading osImage
      "Talos (v1.12.6)". omnictl reports unknown (server version not discoverable from
      the repo — the honest-gap behaviour, by design). On-demand, no scheduled watcher
      (decision d3). Guarded by a test-scripts CI job (43 tests). Live manual proof.'
  - id: fr-isolation-env-in-pods
    capability: In-pod fr isolation (host-worktree mode)
    acceptance: Agent processes in all four agent-bearing containers (secure-agent-pod
      kali+vk-local, hermes-agent-shell hermes+ssh) observe FR_ISOLATION_TARGET=worktree,
      enabling super-fr host-worktree isolation in-pod.
    origin:
    - frank:docs/superpowers/specs/2026-07-24--agents--fr-isolation-target-env-design.md
    levels: {}
    status: skipped
    notes: Live manual proof 2026-07-24 post-PR#687 roll — kubectl exec in all four
      containers (kali, vk-local, hermes, ssh) printed FR_ISOLATION_TARGET=worktree.
      Guard test scripts/tests/test_fr_isolation_target_env.py pins the manifests
      in CI-less local runs.
  - id: fr-isolation-env-ssh-login
    capability: In-pod fr isolation (host-worktree mode)
    acceptance: Agents logging in over SSH/Mosh on either pod also see FR_ISOLATION_TARGET=worktree
      despite sshd env scrubbing (profile.d re-export shims).
    origin:
    - frank:docs/superpowers/specs/2026-07-24--agents--fr-isolation-target-env-design.md
    levels: {}
    status: skipped
    notes: Live manual proof 2026-07-24 — env -i bash -lc printed worktree on kali
      (environ-loop shim, s6 PID 1, post-#687 roll) and on hermes ssh (static-export
      shim, PR#689 + rollout restart; /proc/1/environ is proctitle junk there since
      sshd is PID 1). Guard test pins both shims.
