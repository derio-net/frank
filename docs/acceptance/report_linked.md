# Acceptance coverage — frank

Business-level acceptance tests × verification level.

_66 rows · links: github · generated by `fr acceptance report` from `docs/acceptance/matrix.yaml` (66 rows)._

| status | count |
| --- | ---: |
| CI · per-PR | 3 |
| CI · scheduled | 0 |
| skipped — backfill owed | 53 |
| not implemented | 10 |
| FAILING | 0 |

## 01 · gpu-1 network stability

| Acceptance | Origin | Levels | Automation | Evidence / notes |
| --- | --- | --- | --- | --- |
| Operators have a documented, MAC-bound path to move gpu-1 from the damaged onboard Realtek NIC to the replacement USB 2.5G Ethernet adapter without changing node identity. | [frank:2026-07-09--gpu--gpu-1-usb-25g-nic-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-09--gpu--gpu-1-usb-25g-nic-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | not implemented | Repo-prep row; verified by plan phase 1 docs/template validation. |
| gpu-1 keeps serving the same Kubernetes node identity and 192.168.55.31 address through the replacement USB 2.5G Ethernet adapter. | [frank:2026-07-09--gpu--gpu-1-usb-25g-nic-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-09--gpu--gpu-1-usb-25g-nic-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live manual proof 2026-07-11 — ConfigPatch 404-gpu1-usb-25g-nic applied; gpu-1 Ready on 192.168.55.31 via enp0s20f0u7 MAC 6c:1f:f7:c6:e0:da. |
| Talos detects the USB 2.5G Ethernet adapter with a usable driver, stable link state, and discoverable MAC address before the concrete network patch is applied. | [frank:2026-07-09--gpu--gpu-1-usb-25g-nic-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-09--gpu--gpu-1-usb-25g-nic-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live manual proof 2026-07-11 — Talos detected r8152 USB adapter enp0s20f0u7 with MAC 6c:1f:f7:c6:e0:da; missing rtl8156b firmware patch logged but link works. |
| gpu-1's Talos schematic ships the RTL8156B firmware (rtl_nic/rtl8156b-2.fw) for the USB 2.5G Ethernet adapter, so the r8152 driver loads it cleanly with no "Direct firmware load ... failed" warning at boot. | [frank:2026-07-09--gpu--gpu-1-usb-25g-nic-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-09--gpu--gpu-1-usb-25g-nic-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live manual proof 2026-07-16 — siderolabs/realtek-firmware (v20260309) applied to 402-gpu1-nvidia-extensions.yaml via omnictl; Omni recomputed schematic 54e23848, reinstalled + rebooted gpu-1 (~76s NotReady). Post-boot: extension present, ZERO rtl_nic firmware-load failures in dmesg (both rtl8156b USB and rtl8125b onboard cleared), gpu-1 Ready on 192.168.55.31 (USB NIC re-enumerated enp0s20f0u7->enp0s20f0u5, MAC-bound patch kept the address), nvidia allocatable 1, 19/19 pods back, cilium datapath OK. |
| Cilium on gpu-1 initializes and keeps a healthy direct-routing datapath on the replacement NIC, with no layer-1 NIC flap alert breach during the soak. | [frank:2026-07-09--gpu--gpu-1-usb-25g-nic-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-09--gpu--gpu-1-usb-25g-nic-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | not implemented | Manual post-swap Cilium verification plus 24h carrier-change soak. |

## 02 · gpu-1 workload continuity

| Acceptance | Origin | Levels | Automation | Evidence / notes |
| --- | --- | --- | --- | --- |
| After the NIC maintenance event, gpu-1 returns with NVIDIA allocatable GPU capacity and hard-pinned GPU/agent workloads recover to their intended state. | [frank:2026-07-09--gpu--gpu-1-usb-25g-nic-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-09--gpu--gpu-1-usb-25g-nic-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live manual proof 2026-07-11 — gpu-1 allocatable nvidia.com/gpu is 1; pinned workloads are Running or Completed after the network move. |

## 03 · Network isolation

| Acceptance | Origin | Levels | Automation | Evidence / notes |
| --- | --- | --- | --- | --- |
| Every Frank host has the internal DNS resolvers (192.168.10.11/12) set declaratively in its Talos machine config, so no host silently depends on DHCP-provided DNS. | [frank:2026-07-12--net--declarative-nameservers-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/implemented/specs/2026-07-12--net--declarative-nameservers-design.md>) | [UNIT](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_cluster_wide_nameservers.py>) ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live proof 2026-07-12: 102-cluster-nameservers applied via omnictl to all 7 Frank nodes (non-disruptive); talosctl get resolvers on gpu-1/mini-1/2/3/pc-1/raspi-1/2 all show 192.168.10.11 + 192.168.10.12 from the config layer. Unit guard scripts/tests/test_cluster_wide_nameservers.py passing (local guard, not CI-run).<br>[frank:test_cluster_wide_nameservers.py](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_cluster_wide_nameservers.py>) |
| A static-networked Frank host (e.g. gpu-1 on its USB adapter) resolves DNS via the internal resolvers and boots cleanly, with no fallback to ACL-blocked public DNS. | [frank:2026-07-12--net--declarative-nameservers-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/implemented/specs/2026-07-12--net--declarative-nameservers-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live proof 2026-07-12: gpu-1 (static USB-adapter host) rebooted via talosctl reboot — DOWN 16:51 -> Ready 16:53 (~2min, no time-sync boot-hang); resolvers 192.168.10.11/12 from the config layer; siderolink reconnected; GPU workloads rescheduled Running. The exact incident scenario now boots clean. |
| gpu-1 presents a single declarative IPv4 address on its USB adapter (no DHCP-drift 192.168.55.150 alongside the static 192.168.55.31). | [frank:2026-07-12--net--declarative-nameservers-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/implemented/specs/2026-07-12--net--declarative-nameservers-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live proof 2026-07-12: after console-drift cleanup, talosctl -n 192.168.55.31 get addresses shows a single 192.168.55.31/24 on enp0s20f0u7 (no .150), and it persisted across the gpu-1 reboot-survival test. |

## 04 · Hermes dashboard exposure

| Acceptance | Origin | Levels | Automation | Evidence / notes |
| --- | --- | --- | --- | --- |
| The Hermes web dashboard (hermes.cluster.derio.net) and gateway API (hermes-api.cluster.derio.net) are reachable through a Traefik IngressRoute gated by ip-allowlist + security-headers, with user authentication provided by the dashboard's own basic-auth (frank#626) rather than authentik forward-auth. | [frank:ingressroutes.yaml](<https://github.com/derio-net/frank/blob/main/apps/traefik/manifests/ingressroutes.yaml>) [frank:2026-06-03--orch--hermes-agent-shell-deploy-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/implemented/specs/2026-06-03--orch--hermes-agent-shell-deploy-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | not implemented | 2026-07-12 (willikins#285): authentik forward-auth removed from both hermes routes — no authentik application/proxy-provider was ever configured for these hosts, so the outpost 404'd every request. Mechanism is now the dashboard's own basic-auth (HERMES_DASHBOARD_BASIC_AUTH_*, frank#626; self-auth proven internally :9119/login -> 200). In-repo surface exists; becomes live-provable once this PR merges and ArgoCD reconciles the traefik-system IngressRoutes. SSO via authentik is a possible later follow-up. |

## 05 · Frank alert triage

| Acceptance | Origin | Levels | Automation | Evidence / notes |
| --- | --- | --- | --- | --- |
| The operator can invoke the frank-alert-triage skill and receive a per-alert verdict table classifying every firing Grafana alert (muted-canary, by-design, false-positive, known-tracker, or unexplained) without mutating the cluster. | [frank:2026-07-13--obs--alert-triage-and-agent-report-fixes-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/implemented/specs/2026-07-13--obs--alert-triage-and-agent-report-fixes-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live proof 2026-07-13: ran the frank-alert-triage skill against the live firing set — 2 cert-canaries muted, Layer 11 by-design (frank-ops#11), zero mutation; verdict table matched the manual triage. classify.py unit-tested across 8 decision-tree branches. github_issue is implemented as an orthogonal tracker annotation, not a benign known-tracker verdict, so a live NotReady pod still escalates. |

## 06 · alert-agent report quality

| Acceptance | Origin | Levels | Automation | Evidence / notes |
| --- | --- | --- | --- | --- |
| The alert-agent attributes a traffic surge from evidence (top referrers/user-agents) or states undetermined; it never defaults to a named source such as Hacker News. | [frank:2026-07-13--obs--alert-triage-and-agent-report-fixes-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/implemented/specs/2026-07-13--obs--alert-triage-and-agent-report-fixes-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live proof 2026-07-13: the Hacker News anchor was removed from SKILL.md and the orchestration.py surge/digest prompts (verified live in the rolled pod); a live surge reply attributed the traffic to a Chrome/120 stale-UA crawler from evidence, with no Hacker News. Prompt-content unit tests enforce the absence. (#631, #633) |
| Operator DM and digest replies from the alert-agent are compact plain-text tables, never a raw JSON envelope or a prose wall. | [frank:2026-07-13--obs--alert-triage-and-agent-report-fixes-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/implemented/specs/2026-07-13--obs--alert-triage-and-agent-report-fixes-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live proof 2026-07-13: drove session_send then render_payload end-to-end in the deployed pod — the agent's rich dict result renders as a compact plain-text table (aligned as_of/gpu_mode/alerts_firing/edge_traffic_level rows), raw-JSON=false. Fixed mechanically in render_payload (#634) after instruction-only attempts (#631 removed then #633 restored the text envelope) proved unable to override the session server's per-turn raw-JSON wrapper. Bridge unit tests cover the table render. |
| A focused explicit DM question to the alert-agent returns a real answer within the turn budget, not a timeout fallback. | [frank:2026-07-13--obs--alert-triage-and-agent-report-fixes-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/implemented/specs/2026-07-13--obs--alert-triage-and-agent-report-fixes-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live proof 2026-07-13: a focused DM turn returned status=ok within the bounded DM_TIMEOUT_S=150 (no timeout fallback). An overrun is mechanically backstopped by a deterministic frank-facts surge-compute snapshot, and a transport error can no longer silently drop the reply (#633). Bridge unit tests cover both the fallback and the exception path. |

## 07 · CNC staging secret delivery

| Acceptance | Origin | Levels | Automation | Evidence / notes |
| --- | --- | --- | --- | --- |
| The three CNC staging secrets (cnc-secrets, cnc-ghcr-pull, cnc-runner-auth) are present inside the cnc-staging vCluster with their expected names and keys, so cncd/node/fru can mount them. | [frank:2026-07-19--secrets--cnc-staging-vcluster-secret-sync-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-19--secrets--cnc-staging-vcluster-secret-sync-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live manual proof 2026-07-19 — all 3 secrets present inside the cnc-staging vCluster ns cnc-staging with correct keys (cnc-secrets/CNCD_SECRETS_MASTER_KEY, cnc-runner-auth/token, cnc-ghcr-pull/.dockerconfigjson) via fromHost.secrets after operator populated Infisical (prod env) + forced-sync/restart. No CI (host ESO + live vCluster required). |
| The cnc-ghcr-pull secret synced into the cnc-staging vCluster retains type kubernetes.io/dockerconfigjson, so CNC image pulls from ghcr succeed. | [frank:2026-07-19--secrets--cnc-staging-vcluster-secret-sync-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-19--secrets--cnc-staging-vcluster-secret-sync-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live manual proof 2026-07-19 — cnc-ghcr-pull synced into the cnc-staging vCluster (ns cnc-staging) RETAINED type kubernetes.io/dockerconfigjson (the primary risk); key .dockerconfigjson present. Verified via the vCluster kubeconfig. No CI (live vCluster required). |
| CNC prod secret delivery (ExternalSecrets resolved directly in host namespace cnc) is unchanged by the staging secret-sync work. | [frank:2026-07-19--secrets--cnc-staging-vcluster-secret-sync-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-19--secrets--cnc-staging-vcluster-secret-sync-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Statically guarded by scripts/tests/test_cnc_staging_host_secrets.py::test_prod_overlay_still_includes_the_three_externalsecrets (prod overlay still renders all 3 ExternalSecrets; cnc-base byte-unchanged — the staging change is a staging-scoped $patch:delete). Local guard (frank does not CI-run scripts/tests/); prod runtime path untouched by this PR. |

## 08 · stoa CI on Frank

| Acceptance | Origin | Levels | Automation | Evidence / notes |
| --- | --- | --- | --- | --- |
| Every private agentic-stoa repo has a Gitea mirror on Frank whose main branch tracks GitHub automatically. | [frank:2026-07-19--cicd--stoa-full-mirror-gitea-actions-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-19--cicd--stoa-full-mirror-gitea-actions-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live proof 2026-07-20 — all 10 private repos on Gitea; second-brain/hermes-brain/flexible-health AND the cnc trio (discovered absent, never provisioned since June) created + sha-verified backfilled via scripts/tmp/phase5-gitea-actions.sh. |
| The Actions workflows of the five CI-bearing agentic-stoa repos execute on Frank via Gitea Actions, on PR sync branches, main pushes, and their schedules. | [frank:2026-07-19--cicd--stoa-full-mirror-gitea-actions-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-19--cicd--stoa-full-mirror-gitea-actions-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live proof 2026-07-20 — smoke PR second-brain#12 sync-pr-12; tests/unittest SUCCESS on act-runner; 14+ tasks executed across second-brain/hermes-brain/cnc-frd. Schedule-tick observation still pending. |
| A Frank-side CI run reports its result as a commit status on the originating GitHub PR sha, keeping branch protection meaningful. | [frank:2026-07-19--cicd--stoa-full-mirror-gitea-actions-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-19--cicd--stoa-full-mirror-gitea-actions-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live proof 2026-07-20 — gitea-actions/* statuses on GitHub sha 24f4291c (pending then success/failure transitions) via stoa-status-bridge, 6 PipelineRuns Succeeded. Required frank#664 (EL apply freeze) first. |
| While GitHub Actions and Frank CI run in parallel, mutating jobs (PR robots, tags, releases, issue upserts) execute from exactly one operator-selectable authority side. | [frank:2026-07-19--cicd--stoa-full-mirror-gitea-actions-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-19--cicd--stoa-full-mirror-gitea-actions-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | not implemented | Guards merged in all 5 repos + CI_AUTHORITY=github org variable live (phase5 script). Stays open until the negative is observed — first Gitea-side pins-update tick skipping cleanly and no duplicate robot PRs during the August parallel week. |

## 09 · alert-agent report formatting

| Acceptance | Origin | Levels | Automation | Evidence / notes |
| --- | --- | --- | --- | --- |
| A list-of-uniform-dicts payload renders as an aligned monospace column table in Telegram, not escaped one-line JSON. | [frank:2026-07-22--obs--telegram-report-tables-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-22--obs--telegram-report-tables-design.md>) | [UNIT](<https://github.com/derio-net/frank/blob/main/apps/alert-agent/telegram-bridge/tests/test_bridge.py>) ~~API~~ ~~INT~~ ~~UI~~ | skipped | Unit-proven by render_report tests (aligned table, ragged-key union columns, first-seen order) over the captured live /edge_traffic fixture. Local guard — frank does not CI-run apps/*/tests; live-observed post-merge (report-tables-live-observed).<br>[frank:test_bridge.py](<https://github.com/derio-net/frank/blob/main/apps/alert-agent/telegram-bridge/tests/test_bridge.py>) |
| Over-cap lists show a +N more footer; no value is ever cut mid-character or mid-JSON. | [frank:2026-07-22--obs--telegram-report-tables-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-22--obs--telegram-report-tables-design.md>) | [UNIT](<https://github.com/derio-net/frank/blob/main/apps/alert-agent/telegram-bridge/tests/test_bridge.py>) ~~API~~ ~~INT~~ ~~UI~~ | skipped | Unit-proven — 11-row fixture renders 10 rows + "+1 more", each shown value whole; scalar-list entries appear whole (the 200-char json-truncation defect is gone). Local guard (no CI run); live-observed post-merge.<br>[frank:test_bridge.py](<https://github.com/derio-net/frank/blob/main/apps/alert-agent/telegram-bridge/tests/test_bridge.py>) |
| Values containing angle brackets or ampersands deliver (never a silent HTML-400), proven by unit test and the 400-to-plain fallback path. | [frank:2026-07-22--obs--telegram-report-tables-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-22--obs--telegram-report-tables-design.md>) | [UNIT](<https://github.com/derio-net/frank/blob/main/apps/alert-agent/telegram-bridge/tests/test_bridge.py>) ~~API~~ ~~INT~~ ~~UI~~ | skipped | Unit-proven — every value html.escaped (only <pre> tags are literal angle brackets); send_reply retries once as plain text on a canned 400, data preserved. Local guard (no CI run); the escape path is exercised end-to-end live post-merge.<br>[frank:test_bridge.py](<https://github.com/derio-net/frank/blob/main/apps/alert-agent/telegram-bridge/tests/test_bridge.py>) |
| A live command turn in the deployed pod renders a real monospace table (operator-observed, post-merge). | [frank:2026-07-22--obs--telegram-report-tables-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-22--obs--telegram-report-tables-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live proof 2026-07-22 (PR 668 merged, ArgoCD synced fb6e41c, pod rolled onto bridge-cm with render_report/send_reply/_split_for_telegram present). Drove a realistic /edge_traffic payload through the DEPLOYED render_payload then send_reply, producing aligned monospace column tables, a ragged COUNTRY column with blank cells, a value with angle-brackets and ampersand HTML-escaped, sendMessage parse_mode=HTML. Fed the exact deployed code path (the agent turn itself hit the pre-existing DM latency ceiling, out of scope here). Local guard — no CI run. |

## 10 · alert-agent credential expiry

| Acceptance | Origin | Levels | Automation | Evidence / notes |
| --- | --- | --- | --- | --- |
| When the refresh token is 7 or fewer days from expiry, the daily check sends a Telegram warning whose urgency escalates at 3, 1, and expired. | [frank:2026-07-22--obs--cred-expiry-alert-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-22--obs--cred-expiry-alert-design.md>) | [UNIT](<https://github.com/derio-net/frank/blob/main/apps/alert-agent/handlers/tests/test_cred_expiry.py>) ~~API~~ ~~INT~~ ~~UI~~ | skipped | Unit-proven — evaluate_expiry tier boundaries (8→ok, 7→notice, 3→soon, 1→urgent, 0/-5→expired), should_warn iff tier!=ok, escalating plain-text wording; run_cred_check sends the warning via tg_send on threshold. Local guard (frank does not CI-run apps/*/tests); delivery observed live post-merge (cred-expiry-live-observed).<br>[frank:test_cred_expiry.py](<https://github.com/derio-net/frank/blob/main/apps/alert-agent/handlers/tests/test_cred_expiry.py>) |
| A Grafana rule pages Telegram directly if the daily heartbeat line stops appearing in VictoriaLogs (checker died or pod down). | [frank:2026-07-22--obs--cred-expiry-alert-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-22--obs--cred-expiry-alert-design.md>) | [UNIT](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_cred_expiry_alert_rule.py>) ~~API~~ ~~INT~~ ~~UI~~ | skipped | Rule shape unit-pinned — VictoriaLogs stats query on the _msg field (NOT Hop's log field; verified live), lt 1 + noDataState OK, telegram_direct, 30h window, for 2h. Query validated server-side against live VictoriaLogs (returns 0 pre-deploy). Local guard; end-to-end firing observed live post-merge.<br>[frank:test_cred_expiry_alert_rule.py](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_cred_expiry_alert_rule.py>) |
| A missing, unparseable, or field-less credential file yields a warning plus an error-tier heartbeat, never a silent skip. | [frank:2026-07-22--obs--cred-expiry-alert-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-22--obs--cred-expiry-alert-design.md>) | [UNIT](<https://github.com/derio-net/frank/blob/main/apps/alert-agent/handlers/tests/test_cred_expiry.py>) ~~API~~ ~~INT~~ ~~UI~~ | skipped | Unit-proven — missing/unparseable/field-less/bool/Infinity/NaN/out-of-range-epoch + non-FileNotFound read + invalid-UTF-8 all yield tier=error + a warning + a heartbeat; evaluate_expiry is total (never raises) and run_cred_check always heartbeats even on an unexpected error. Local guard; no CI run.<br>[frank:test_cred_expiry.py](<https://github.com/derio-net/frank/blob/main/apps/alert-agent/handlers/tests/test_cred_expiry.py>) |
| Post-merge, both the warning and the dead-man heartbeat signals are triggered and observed live. | [frank:2026-07-22--obs--cred-expiry-alert-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-22--obs--cred-expiry-alert-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live proof 2026-07-22 (after PR 671 plus the PR 673 nested-field fix rolled). Heartbeat run against the REAL cred gave days_left=27 tier=ok; seeded to VictoriaLogs (dead-man query value=1). Forced 2-days-out cred via CRED_PATH delivered a real Telegram warning (tier=urgent). Grafana rule alert-agent-cred-expiry-heartbeat-stale loaded (provisioning API) and evaluates state=inactive/health=ok against the live heartbeat. Live-only. |

## 11 · Agent shells

| Acceptance | Origin | Levels | Automation | Evidence / notes |
| --- | --- | --- | --- | --- |
| Operators driving Talos from an agent shell use a talosctl whose version is supported against the running cluster, instead of one three minor versions behind it. | [frank:2026-07-23--agents--agent-images-upstream-version-sweep-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-23--agents--agent-images-upstream-version-sweep-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | VERIFIED LIVE 2026-07-23 — secure-agent-pod kali container (image secure-agent-kali:c7a80f6) reports talosctl client Tag: v1.12.6, matching the cluster (all 7 nodes Talos v1.12.6). The three-minor skew (was v1.9.5, outside Talos's supported +/-1) is closed. The audit script's cluster anchor also resolves to v1.12.6 and marks the pin ok. Live manual proof; no CI level. |
| Every agent shell Frank deploys still boots and serves after the upstream version sweep rebuilds it. | [frank:2026-07-23--agents--agent-images-upstream-version-sweep-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-23--agents--agent-images-upstream-version-sweep-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | VERIFIED LIVE 2026-07-23 — all rebuilt shells Running on c7a80f6: alert-agent (3 multi-agent-shell containers, s6-rc started clean, no crashloop-bail), n8n-01 (multi-agent-shell sidecar), secure-agent-pod (kali + vk-local 2/2), paperclip (paperclip-shell 2/2), ruflo (ruflo-server + ruflo-shell 2/2), hermes-agent-shell (hermes + hindsight + ssh 3/3). cnc-base NOT covered: its runner StatefulSet is not currently deployed (cnc-prod app OutOfSync/Missing, pre-existing — the manual-login persistent agent is down, unrelated to the sweep). Live manual proof. |
| A Hermes version bump actually reaches the running shell, rather than shipping in the image while the pod keeps serving the old venv off its PVC. | [frank:2026-07-23--agents--agent-images-upstream-version-sweep-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-23--agents--agent-images-upstream-version-sweep-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | VERIFIED LIVE 2026-07-23, with a topology correction the live check surfaced. The DEPLOYED consumer of the bumped image is the hermes-agent-shell-ssh sidecar (built FROM docker v2026.7.20): its running container reports "Hermes Agent v0.19.0 (2026.7.20)" — the bump reached the pod. TWO nuances the spec's image->app mapping got wrong: (1) the base hermes-agent-shell image (PyPI 0.19.0, the .seed-version PVC-venv one this row was written about) is NOT deployed anywhere in frank — same status as infra-shell — so its bump is validated by CI smoke only; (2) the pod's runtime "hermes" container is docker.io/nousresearch/hermes-agent:v2026.7.7.2 (=0.18.2), pinned DIRECTLY in the manifest, NOT a ghcr.io/derio-net/* image, so the sweep did not touch it. Consequence: the intent_ack_continuation manual-op (frank plan P2.T2.S7) is moot for the current deployment — see the plan note. Live manual proof. |

## 12 · Ruflo

| Acceptance | Origin | Levels | Automation | Evidence / notes |
| --- | --- | --- | --- | --- |
| Ruflo still serves its UI and handles file upload and download after the upstream re-vendor, proving the local GridFS and wasm-scheme patches survived. | [frank:2026-07-23--agents--agent-images-upstream-version-sweep-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-23--agents--agent-images-upstream-version-sweep-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | VERIFIED LIVE 2026-07-23 — ruflo Running on ruflo-server:c7a80f6, readiness 1/1, /api/v2/feature-flags returns valid JSON. Both local patches confirmed present in the RUNNING build: the GridFS shim (Readable.from / new Writable) compiled into /app/build/server/chunks/database-*.js, and the wasm allow-line in urlSafety-*.js. The re-vendor (a6dd4ab->26c35b5) preserved both. package.json version 0.20.0. Live manual proof (endpoint + compiled-patch presence); a full authenticated file round-trip was not exercised, but the patch code is demonstrably in the artifact. |

## 13 · Agent image maintenance

| Acceptance | Origin | Levels | Automation | Evidence / notes |
| --- | --- | --- | --- | --- |
| An operator can re-measure how far every agent-images pin has drifted from upstream on demand, without redoing the manual registry-by-registry reconnaissance. | [frank:2026-07-23--agents--agent-images-upstream-version-sweep-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-23--agents--agent-images-upstream-version-sweep-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | VERIFIED LIVE 2026-07-23 — scripts/version_audit.py (agent-images main c7a80f6) run against the live cluster reports every bumped pin ok, and its talosctl anchor resolves to the cluster's v1.12.6 (not GitHub latest v1.13.7), reading osImage "Talos (v1.12.6)". omnictl reports unknown (server version not discoverable from the repo — the honest-gap behaviour, by design). On-demand, no scheduled watcher (decision d3). Guarded by a test-scripts CI job (43 tests). Live manual proof. |

## 14 · In-pod fr isolation (host-worktree mode)

| Acceptance | Origin | Levels | Automation | Evidence / notes |
| --- | --- | --- | --- | --- |
| Agent processes in all four agent-bearing containers (secure-agent-pod kali+vk-local, hermes-agent-shell hermes+ssh) observe FR_ISOLATION_TARGET=worktree, enabling super-fr host-worktree isolation in-pod. | [frank:2026-07-24--agents--fr-isolation-target-env-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-24--agents--fr-isolation-target-env-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live manual proof 2026-07-24 post-PR#687 roll — kubectl exec in all four containers (kali, vk-local, hermes, ssh) printed FR_ISOLATION_TARGET=worktree. Guard test scripts/tests/test_fr_isolation_target_env.py pins the manifests in CI-less local runs. |
| Agents logging in over SSH/Mosh on either pod also see FR_ISOLATION_TARGET=worktree despite sshd env scrubbing (profile.d re-export shims). | [frank:2026-07-24--agents--fr-isolation-target-env-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-24--agents--fr-isolation-target-env-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live manual proof 2026-07-24 — env -i bash -lc printed worktree on kali (environ-loop shim, s6 PID 1, post-#687 roll) and on hermes ssh (static-export shim, PR#689 + rollout restart; /proc/1/environ is proctitle junk there since sshd is PID 1). Guard test pins both shims. |

## 15 · Resource Metrics API

| Acceptance | Origin | Levels | Automation | Evidence / notes |
| --- | --- | --- | --- | --- |
| An operator can run 'kubectl top nodes' and 'kubectl top pods -A' and see live CPU/memory utilization for all 7 nodes (incl. both raspis) and pods across namespaces. | [frank:2026-07-25--obs--metrics-api-server-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/implemented/specs/2026-07-25--obs--metrics-api-server-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Served by metrics-server (metrics.k8s.io). Live proof 2026-07-25 (PR #699 merged, root App-of-Apps synced to f530f0dd, metrics-server app Synced): kubectl top nodes returned all 7 nodes with live CPU/mem incl. both arm64 raspis (raspi-1 483m/12%/1663Mi, raspi-2 603m/15%/1861Mi, gpu-1 4785m/14%, mini-1/2/3, pc-1); kubectl top pods -A non-empty across namespaces; v1beta1.metrics.k8s.io APIService Available=True; metrics-server logs clean (no x509/scrape errors). Live manual proof — no CI level (frank does not run kubectl-top in CI). |
| A CPU/memory HorizontalPodAutoscaler can read live utilization (HPA TARGETS shows a real percentage, not <unknown>) and act on it. | [frank:2026-07-25--obs--metrics-api-server-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/implemented/specs/2026-07-25--obs--metrics-api-server-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live proof 2026-07-25: a throwaway CPU HPA on monitoring/blackbox-exporter (idle, 10m CPU request) reported TARGETS resolving from <unknown> to a real 40%/80% percentage once the HPA controller queried the Metrics API, proving resource metrics feed the HPA control loop; HPA then deleted (no standing HPA ships) and blackbox-exporter stayed 1/1 (no scale drift). Live manual proof — no CI level. |

## 16 · Public marketing site (www.derio.net)

| Acceptance | Origin | Levels | Automation | Evidence / notes |
| --- | --- | --- | --- | --- |
| A visitor to https://www.derio.net receives the published site page over valid TLS, served from a versioned container image rather than a hard-coded Caddy string. | [frank:2026-07-25--edge--stoa-site-www-derio-net-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/implemented/specs/2026-07-25--edge--stoa-site-www-derio-net-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live proof 2026-07-26 - https://www.derio.net returns HTTP/2 200 over valid TLS serving the built page (867 bytes of Astro output, external stylesheet applied), from image ghcr.io/agentic-stoa/site:<sha> rather than the old 12-byte respond-literal. Image is a single-arch amd64/linux manifest matching hop-1; the tag in clusters/hop/apps/www was written by the site-promotion pipeline, not by hand. Test Plan step 1. |
| A content change pushed to the private site repo reaches www.derio.net automatically, without an operator touching the Frank or Hop clusters. | [frank:2026-07-25--edge--stoa-site-www-derio-net-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/implemented/specs/2026-07-25--edge--stoa-site-www-derio-net-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live proof 2026-07-26 - observed end to end TWICE, the second time fully unattended. A page-content change pushed to the site repo main produced GitHub webhook 202, then agentic-stoa-main-sync, then the Gitea mirror at the same sha, then Gitea Actions green, then the GHCR image, then a site-promotion PipelineRun, then commit 74e9874c on frank main promoting the www image, then ArgoCD on Hop, then pod 1/1 on the new tag, then the changed page live. No operator touched either cluster. Required an UNPLANNED fix first - the Gitea push webhook to el-gitea-listener did not exist, so the promotion trigger had no delivery path (manual-op cicd-stoa-site-gitea-push-webhook). Test Plan step 6. |
| Both public sites (www.derio.net and blog.derio.net) return hardened response headers — HSTS, nosniff, Referrer-Policy, Permissions-Policy and a Content-Security-Policy — without breaking analytics. | [frank:2026-07-25--edge--stoa-site-www-derio-net-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/implemented/specs/2026-07-25--edge--stoa-site-www-derio-net-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live proof 2026-07-26 - both www.derio.net and blog.derio.net return HSTS (max-age=31536000, includeSubDomains, deliberately NO preload), nosniff, Referrer-Policy, Permissions-Policy and the CSP, with the Server header stripped. Analytics unbroken and the CSP proven ENFORCED rather than merely present - in a real browser the GoatCounter beacon fired on path / while an injected inline style element was blocked (style-src-elem violation), with zero violations from the page itself. Also verified on the handle_errors FALLBACK path, which originally served with all five headers missing and leaked the Server header - fixed in PR 705 and re-verified by scaling the backend to 0. Test Plan steps 2 and 3. |
| If www.derio.net stops answering, the operator is alerted through the same Layer 17 edge path that already covers the blog. | [frank:2026-07-25--edge--stoa-site-www-derio-net-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/implemented/specs/2026-07-25--edge--stoa-site-www-derio-net-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live proof 2026-07-26 - probe_success for the www instance exists in the feature_health group and the layer-17-edge-down matcher returns exactly the two edge instances (blog and www), excluding the neighbouring frank.derio.net probes. The KNOWN LIMIT recorded when this row was written is now CLOSED by P6.T2.S4 - the probe uses the content-asserting www_content module, so with the backend scaled to 0 probe_success went from 1 to 0 within 20s, where the old status-only module would have stayed at 1 forever against the fallback 200. Recovered to 1 within 30s of scale-up. NOT held for the rule for:5m - layer-17-edge-down is severity:critical with github_issue frank-ops#17, so a deliberate fire would page and file a bug; rule coverage was proven independently by the matcher query. Test Plan step 5. |
| Requests to www.derio.net are searchable in central logging and counted in analytics, and abusive clients are blocked at the edge by CrowdSec. | [frank:2026-07-25--edge--stoa-site-www-derio-net-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/implemented/specs/2026-07-25--edge--stoa-site-www-derio-net-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live proof 2026-07-26 - VictoriaLogs returned 77 hits for the www.derio.net request host scoped to kubernetes.host hop-1 (the vhost new log directive); a real browser fired the GoatCounter beacon at counter.derio.net with path /; and CrowdSec caddy-logs cs_node_hits_ok_total rose by 11 across 10 requests to www, confirming the crowdsec directive parses the new vhost access log. Test Plan steps 3, 4 and 7. |

## 17 · kid-laptops CI

| Acceptance | Origin | Levels | Automation | Evidence / notes |
| --- | --- | --- | --- | --- |
| A push to derio-homelab/kid-laptops on GitHub reliably appears at the same commit SHA in the in-cluster Gitea mirror. | [frank:2026-07-26--cicd--kid-laptops-tekton-ci-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-26--cicd--kid-laptops-tekton-ci-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live proof 2026-07-27 — push to GitHub main, mirror PipelineRun kid-laptops-main-sync-z4gh6 Succeeded, and the Gitea mirror head matched GitHub at the identical SHA (5b895328edcf, later a72a3badc84a). Skipped rather than ci: the delivery path is forge webhook state with no IaC, so a webhook deleted by hand in either forge would break this silently. apps/tekton/webhooks.yaml declares the desired paths and scripts/tests/test_webhook_delivery_paths.py enforces the declaration at PR time, but nothing reconciles it against the live forges yet. |
| A push to the Gitea mirror creates exactly one kid-laptops PipelineRun in kid-laptops-ci, and no second run from the generic gitea-ci trigger. | [frank:2026-07-26--cicd--kid-laptops-tekton-ci-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-26--cicd--kid-laptops-tekton-ci-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live proof 2026-07-27 — five real pushes produced exactly one PipelineRun each in kid-laptops-ci and ZERO generic gitea-ci runs. The 'exactly one' half guarded a real defect: the generic gitea-push trigger's agentic-stoa-only exclusion would also have matched derio-homelab, adding a second failing run per push under a generic name. The manifest half IS enforced in frank CI by scripts/tests/test_kid_laptops_pipelinerun_contract.py (since #707); the live half needs a real push, hence skipped rather than ci. |
| Every push to kid-laptops runs lint, ansible syntax-check and unit tests against the pushed commit, and the run is green when they pass. | [frank:2026-07-26--cicd--kid-laptops-tekton-ci-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-26--cicd--kid-laptops-tekton-ci-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live proof 2026-07-27 — PipelineRun kid-laptops-ci-fqs2h, 51s: clone, lint, syntax and unit-tests all Succeeded against the pushed commit (a72a3badc84a). Deliberately narrower than kid-laptops' own every-push-runs-ci-pipeline row: molecule-base and molecule-teardown are Skipped on the enable-molecule gate until KubeVirt lands, so this claims ONLY the Python/Ansible half. Reaching green needed two kid-laptops fixes CI itself surfaced (#48 taskRefs resolved from git, #50 vault shim covering ansible-lint). Not ci: the run is triggered by a push to kid-laptops, not by a frank PR, so a frank-side change cannot be caught here. |
| The kid-laptops-ci-kubevirt ServiceAccount is authorised to create and delete VirtualMachineInstances in the kid-laptops-ci namespace. | [frank:2026-07-26--cicd--kid-laptops-tekton-ci-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-26--cicd--kid-laptops-tekton-ci-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | not implemented | Deliberately still not-implemented. `kubectl auth can-i create virtualmachineinstances` answers yes (checked 2026-07-27) while the API server warns "the server doesn't have a resource type 'virtualmachineinstances'" — RBAC rules are opaque strings and are never resolved against installed CRDs, so that yes is true and proves nothing. Provable only once KubeVirt is deployed (spec 2026-07-26--virt--kubevirt-vms-design.md). |

## 18 · Virtualization

| Acceptance | Origin | Levels | Automation | Evidence / notes |
| --- | --- | --- | --- | --- |
| An operator can run a virtual machine on Frank and reach its console. | [frank:2026-07-26--virt--kubevirt-vms-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-07-26--virt--kubevirt-vms-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | not implemented | Design-only in this run; no KubeVirt on the cluster yet. Deliberately broad — it is the claim the whole virt layer exists to make. |

## 19 · Canonical cluster service access

| Acceptance | Origin | Levels | Automation | Evidence / notes |
| --- | --- | --- | --- | --- |
| Operators can reach and authenticate to every Frank cluster service using its cluster.derio.net URL without depending on a legacy frank.derio.net route. | [frank:2026-06-01--net--frank-derio-net-retire-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-06-01--net--frank-derio-net-retire-design.md>) | [UNIT](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_frank_domain_retirement.py>) ~~API~~ ~~INT~~ ~~UI~~ | skipped | VERIFIED LIVE 2026-08-03 — services remain reachable on *.cluster.derio.net now that the legacy frank fallback is gone. Spot-checked argocd.cluster.derio.net -> 200, with the *.cluster.derio.net wildcard intact for litellm/harbor/auth/grafana. The homelab master dashboard was repointed and redeployed: 15 tiles now target *.cluster.derio.net with live siteMonitor probes, omni keeps its preserved name, and the Portainer tile plus the rp.frank.derio.net Traefik bookmark are gone. CORRECTION worth recording: litellm.cluster.derio.net is the forward-auth admin UI and 302s an API client to SSO even with a valid Bearer key — so 'canonical' is per-consumer, not per-name. Machine clients use litellm-api.cluster.derio.net (no forward-auth, added in frank#749), verified returning LiteLLM's own 401 on /v1/models and resolving via the mesh resolver; kid-laptops and the operator .env files moved to it in derio-homelab/kid-laptops#81, with a tripwire against regression. BREADTH (2026-08-03, independent re-check): the row claims EVERY service, so the single-host spot-check above was widened to a ten-host sweep, all over valid TLS (curl ssl_verify_result=0) with the legacy fallback already NXDOMAIN and therefore unable to mask a failure — argocd and vk returned HTTP 200; grafana, longhorn, hubble, infisical, paperclip, auth, n8n and tekton returned HTTP 302 to Authentik, which is the correct forward-auth answer for a browserless client and, per the litellm correction above, is a pass for a human consumer rather than a machine one.<br>[frank:test_frank_domain_retirement.py](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_frank_domain_retirement.py>) |
| Existing Kubernetes OIDC credentials continue to authenticate during the issuer migration while newly minted credentials use auth.cluster.derio.net. | [frank:2026-06-01--net--frank-derio-net-retire-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-06-01--net--frank-derio-net-retire-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live manual proof of the dual-issuer overlap. 2026-08-01: an old-issuer k8s-agent token retained from before the cutover kept authenticating under authenticator A while newly minted tokens carried iss=auth.cluster.derio.net, both normalizing to authentik:ak-Kubernetes Agent Access-client_credentials. 2026-08-02T08:51Z (T0+14h55m): re-proved by minting through both hosts — legacy iss=auth.frank.derio.net and cluster iss=auth.cluster.derio.net, both TokenReview authenticated=true with identical normalized identity and groups [system:authenticated], while the Omni service-account kubeconfig retained cluster-admin. Provider access_token_validity measured at hours=8, so every token issued at or before T0=2026-08-01T17:56:02Z expired by 2026-08-02T01:56:02Z. Not automatable in CI: needs a live cluster, the Authentik client secret and TokenReview. |
| Every non-Omni frank.derio.net service name is removed from application configuration, ingress, and DNS after the migration overlap closes. | [frank:2026-06-01--net--frank-derio-net-retire-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-06-01--net--frank-derio-net-retire-design.md>) | [UNIT](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_frank_domain_retirement.py>) ~~API~~ ~~INT~~ ~~UI~~ | skipped | VERIFIED LIVE 2026-08-03 — the DNS half this row was waiting on is done. derio-homelab/ansible-pihole#1 removed the frank.derio.net wildcard A (192.168.55.220); #2 removed the mini-1/2/3.frank.derio.net CNAMEs and the commented future-extension block after confirming nothing in any repo referenced them. Both deployed via dns.yaml. Measured AFTER deploy: 20 retired names x 3 resolvers = 60/60 NXDOMAIN — pihole A (192.168.10.11), pihole B (192.168.10.12), and the Headscale mesh resolver (100.100.100.100) from a mesh-joined host, which is the LAN-and-mesh proof this row demands and which scripts/tmp/p6-verify.sh explicitly cannot make. auth.frank.derio.net specifically stopped resolving to 192.168.55.220, closing the dangling default-cert name. The repository half stays CI-guarded by scripts/tests/test_frank_domain_retirement.py; the homelab side gained its own tripwire (ansible-homelab-automation/scripts/check-retired-domains.sh, wired into `make check`). NOTE: the first dns.yaml run applied a two-commit-stale checkout and exited 0 — only re-querying the names caught it, the same family as the omnictl-reads-a-local-file gotcha recorded elsewhere in this plan.<br>[frank:test_frank_domain_retirement.py](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_frank_domain_retirement.py>) |
| Omni remains reachable and usable at omni.frank.derio.net after all other frank.derio.net service names are retired. | [frank:2026-06-01--net--frank-derio-net-retire-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-06-01--net--frank-derio-net-retire-design.md>) | [UNIT](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_frank_domain_retirement.py>) ~~API~~ ~~INT~~ ~~UI~~ | skipped | VERIFIED LIVE 2026-08-03 — omni.frank.derio.net survived the wildcard removal because an explicit CNAME outlives the wildcard it sat under: it resolves to omni.frank.lan -> 192.168.10.30 on BOTH the LAN and the mesh resolver. Omni UI returns 200 with a genuine Let's Encrypt certificate (CN=omni.frank.derio.net, valid to 2026-10-08 — not a Traefik default cert); discovery returns 200; unauthenticated :8100 returns 401. Certificate renewal — which upstream required be preserved — is owned by proxmox-cluster's omni-cert-renew systemd timer rather than by Traefik, because Omni now runs on the gondor VM with no reverse proxy at all: the timer is active, its last run reported Result=success ExecMainStatus=0, and the next run is scheduled. p6-verify.sh: 37 pass / 0 fail / 0 pending.<br>[frank:test_frank_domain_retirement.py](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_frank_domain_retirement.py>) |

## 20 · Edge & mesh access

| Acceptance | Origin | Levels | Automation | Evidence / notes |
| --- | --- | --- | --- | --- |
| Frank publishes mesh-resolvable names for LiteLLM, and an authenticated API call to them returns a model list rather than being intercepted by the public SSO ingress. | [frank:2026-08-02--edge--litellm-mesh-dns-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-08-02--edge--litellm-mesh-dns-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | PROVEN LIVE 2026-08-02, after manual-op edge-headscale-litellm-mesh-dns-restart. DNS, read on a mesh node with all three controls so the homelab wildcard could not fake it: gitea-ssh.cluster.derio.net -> 192.168.55.209 (positive control: MagicDNS answering and outranking the wildcard), nope-xyz.cluster.derio.net -> 192.168.55.220 (negative control: the wildcard itself), litellm-lb.cluster.derio.net -> 192.168.55.206 (proof: differs from the wildcard, so it can only be the new map). Reachability: http://litellm-lb.cluster.derio.net:4000/health/liveliness -> 200. Route: https://litellm-api.cluster.derio.net/v1/models -> 401 from LiteLLM with a valid wildcard cert (ssl_verify=0), NOT a 302 to Authentik. Authenticated: the same URL with the vaulted kid-laptops virtual key -> 200 and a 12-model list (mistral-small-24b, gemma-12b, ...), which is the load-bearing check - every other result above is satisfied by a LiteLLM that rejects all keys. SSO regression: https://litellm.cluster.derio.net/ still 302s to Authentik. Status is skipped rather than ci because the proof needs a live mesh node and a real credential; the offline half (route shape, no forward-auth) IS ci-guarded under litellm-public-host-keeps-sso. The off-LAN foreign-network leg is derio-homelab/kid-laptops' row laptop-reaches-frank-from-anywhere. |
| Adding mesh-only API endpoints for LiteLLM does not remove SSO from its public hostname, and the mesh endpoints never acquire the forward-auth middleware that would break API-key clients. | [frank:2026-08-02--edge--litellm-mesh-dns-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-08-02--edge--litellm-mesh-dns-design.md>) | [UNIT](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_headscale_litellm_mesh_dns.py#L143>) ~~API~~ ~~INT~~ ~~UI~~ | CI · per-PR | Implemented phase 2. Both directions asserted, because each failure mode is silent in opposite ways: L143 test_litellm_api_route_has_no_forward_auth (the API route never acquires forward-auth — if it did, every Bearer-key client would get a 302 to Authentik, the exact bug this plan routes around) and L167 test_litellm_public_route_still_has_forward_auth (litellm.cluster.derio.net keeps SSO on the admin UI, which someone tidying two near-identical routes could strip). L156 additionally holds ip-allowlist + security-headers on the API route, so "drop forward-auth" cannot quietly become "drop the network guards too". Runs on every PR via .github/workflows/repo-tripwires.yml (pytest scripts/tests/, no paths filter). Both guards were mutation-tested rather than trusted: L167 passes on unmodified HEAD (it guards a property already true), so it was proven to bite by stripping forward-auth from the public route — each mutation failed exactly one test and no others.<br>[frank:test_headscale_litellm_mesh_dns.py](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_headscale_litellm_mesh_dns.py#L143>) [frank:test_headscale_litellm_mesh_dns.py](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_headscale_litellm_mesh_dns.py#L156>) [frank:test_headscale_litellm_mesh_dns.py](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_headscale_litellm_mesh_dns.py#L167>) |

## 21 · Retrieval models

| Acceptance | Origin | Levels | Automation | Evidence / notes |
| --- | --- | --- | --- | --- |
| An in-cluster client can rerank a candidate passage set against a multilingual cross-encoder and receive scored, well-separated result indices. | [frank:2026-08-02--infer--igpu-embedding-rerank-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-08-02--infer--igpu-embedding-rerank-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | MET 2026-08-03 live: POST /v3/rerank returns {results:[{index,relevance_score}]}; scores well separated (0.0395 relevant vs 1.6e-5 irrelevant, ~2400x), not the degenerate 1e-9..1e-12 pattern. Both servables AVAILABLE via GET /v1/config. CI guards the manifest SHAPE (scripts/tests/test_ovms_retrieval_manifests.py), not the serving behaviour, so this row is live-proof rather than ci. |
| Operators have a measured rerank latency figure for a 20-passage batch on the mini iGPU, plus a same-node CPU control arm, instead of an arithmetic estimate. | [frank:2026-08-02--infer--igpu-embedding-rerank-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-08-02--infer--igpu-embedding-rerank-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | MET 2026-08-03 by live measurement (manual, not automatable in CI). iGPU rerank of 20 candidates: p50 77.3ms, p95 87.1ms, max 91.1ms, N=30 after warm-up. CPU control arm on the same node, same weights: p50 1612.9ms, p95 1702.6ms -> iGPU 20.9x faster. Caveat recorded: single-item embedding is only 1.1x (28.4 vs 32.2ms), request-overhead bound; and the ratio is at short sequence length. |
| In-cluster clients can obtain multilingual embeddings from Frank with a recorded vector dimensionality. | [frank:2026-08-02--infer--igpu-embedding-rerank-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-08-02--infer--igpu-embedding-rerank-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | MET 2026-08-03 live: POST /v3/embeddings returns 1024-dimensional vectors, dimension read from the response. Single p50 28.4ms, batch-32 p50 93.2ms, throughput 35.2/343.5 items per s. CI guards the manifest shape only; the serving behaviour is live proof. |

## 22 · Control-plane safety

| Acceptance | Origin | Levels | Automation | Evidence / notes |
| --- | --- | --- | --- | --- |
| A warm inference workload pinned to a control-plane mini leaves etcd quorum and API-server latency unchanged relative to the two untouched peers. | [frank:2026-08-02--infer--igpu-embedding-rerank-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-08-02--infer--igpu-embedding-rerank-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | skipped | PARTIALLY MET 2026-08-03. 7500 rerank requests over 240s (~31/s) from mini-1: apiserver p99 (WATCH/CONNECT excluded) 23.4ms before vs 22.1ms under load; all three minis Ready, no Memory/DiskPressure; ovms working set 2254Mi of 6Gi. NOT VERIFIABLE: etcd leader changes and WAL fsync -- Frank does not scrape etcd (no etcd scrape job; the etcd_* series present are the apiserver's etcd CLIENT metrics). Tracked as a follow-up; the row is deliberately NOT marked fully met. |

## 23 · GPU documentation

| Acceptance | Origin | Levels | Automation | Evidence / notes |
| --- | --- | --- | --- | --- |
| Operators have accurate documentation of how to claim a mini iGPU that matches what is actually deployed (DRA ResourceClaim, not a device-plugin extended resource). | [frank:2026-08-02--infer--igpu-embedding-rerank-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-08-02--infer--igpu-embedding-rerank-design.md>) | [UNIT](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_igpu_dra_docs.py>) ~~API~~ ~~INT~~ ~~UI~~ | CI · per-PR | Test Plan row 8; phase05 README drifted a full API generation. Fixed 2026-08-02 (phase 4): patches/phase05-mini-config/README.md corrected to the DRA resource driver + a worked ResourceClaim example, plus a new docs/runbooks/frank-gotchas/igpu-dra.md. Unit guard scripts/tests/test_igpu_dra_docs.py runs on every PR via .github/workflows/repo-tripwires.yml (pytest scripts/tests/ -q) — which is what status: ci records here, rather than skipped.<br>[frank:test_igpu_dra_docs.py](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_igpu_dra_docs.py>) |

## 24 · Feature-health alerting

| Acceptance | Origin | Levels | Automation | Evidence / notes |
| --- | --- | --- | --- | --- |
| A control-plane rolling reboot completes without any feature-health alert firing for a pod terminated by the reboot. | [frank:2026-08-02--obs--feature-health-workload-metrics-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-08-02--obs--feature-health-workload-metrics-design.md>) | [UNIT](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_feature_health_workload_metrics.py>) ~~API~~ ~~INT~~ ~~UI~~ | not implemented | Phase 6 live run 2026-08-03 (PR #756, merge b8558529): 39 feature-health rules live, ZERO still querying kube_pod_status_ready, and the firing set fell 25 -> 3 (2 TLS canaries + 1 gpu_timeshare) — the exact muted/by-design baseline. STILL not-implemented because the claim is about what happens DURING a control-plane roll, and no roll has occurred since the merge. What is proven: the defect class is absent from the manifests (CI) and from the live ConfigMap. What is not: that a reboot passes quietly. Flips at the next node reboot.<br>[frank:test_feature_health_workload_metrics.py](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_feature_health_workload_metrics.py>) |
| A workload that loses all available replicas still raises its layer's feature-health alert. | [frank:2026-08-02--obs--feature-health-workload-metrics-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-08-02--obs--feature-health-workload-metrics-design.md>) | [UNIT](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_feature_health_workload_metrics.py>) ~~API~~ ~~INT~~ ~~UI~~ | not implemented | Phase 6 live run 2026-08-03: all 14 expressions return series against live VictoriaMetrics, each `or` branch independently, every series carrying workload + lowercase kind, every value 0 — the correct quiet state under gt 0 and the permanently-firing state under the old lt 1, which is the per-rule polarity proof. STILL not-implemented: that proves the queries resolve and are correctly polarised, not that losing replicas RAISES the alert. Needs an injected or genuine failure. Known narrowing recorded in grafana.md: layer-12 no longer watches the Rollout-managed sympozium-apiserver replicas.<br>[frank:test_feature_health_workload_metrics.py](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_feature_health_workload_metrics.py>) |
| The routing-relevant fields of every migrated feature-health alert — folder, uid, severity and github_issue — are unchanged by the rewrite, so each alert reaches exactly the receivers it reached before. | [frank:2026-08-02--obs--feature-health-workload-metrics-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-08-02--obs--feature-health-workload-metrics-design.md>) | [UNIT](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_feature_health_workload_metrics.py>) ~~API~~ ~~INT~~ ~~UI~~ | CI · per-PR | CORRECTED at code review. This row previously claimed these alerts route to the Health Bridge "only, never to Telegram" — that is FALSE, and it was marked status:ci, so CI appeared to prove it. notification-policy-cm.yaml evaluates severity=critical -> Telegram (continue:true) and severity=warning -> Telegram (continue:true) BEFORE the grafana_folder=feature-health -> Health Bridge route, and none of these rules carries an escape-hatch label (gpu_timeshare, health_bridge_only, canary, telegram_direct — all of whose routes deliberately sit above the severity routes). So every one of them delivers to Telegram AND Health Bridge, and did so before this change too. That is why the 2026-08-02 flood was noticed at all. The provable claim is the one now stated: routing INPUTS are unchanged. Enforced on every PR by .github/workflows/repo-tripwires.yml (pytest scripts/tests/ -q, no paths filter): test_migrated_rules_keep_folder_uid_severity_and_tracker pins folder + severity + github_issue for all twelve migrated uids against a captured expected-dict (layer-8 was missing from that map until code review — its folder and tracker were unpinned), test_every_feature_health_rule_lives_in_the_feature_health_folder catches a group-header typo, and test_every_rule_has_a_non_empty_uid_and_uids_are_globally_unique catches a uid collision that would stop a rule evaluating at all. Each assertion was mutation-checked against a doctored ConfigMap.<br>[frank:test_feature_health_workload_metrics.py](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_feature_health_workload_metrics.py>) |
| An unexpectedly scaled-to-0 Deployment raises an alert, while the GPU-timeshared and Rollout-managed workloads that sit at 0 by design do not. | [frank:2026-08-02--obs--feature-health-workload-metrics-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-08-02--obs--feature-health-workload-metrics-design.md>) | [UNIT](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_feature_health_workload_metrics.py>) ~~API~~ ~~INT~~ ~~UI~~ | not implemented | Two-part claim, one part now proven live. PROVEN (2026-08-03, post-merge): the by-design workloads do NOT alert — comfyui (gpu-switcher timeshare) and litellm (Rollout scaleDown: onsuccess, serving 5/5) both sit at 0 replicas and the shipped expr returns 0 series, while the same query with exclusions stripped returns exactly those 2, so it is quiet for the right reason rather than by matching nothing. NOT PROVEN: that an UNEXPECTED scale-to-0 raises the alert — needs a real one. Note the rule sits in Normal (NoData) in the healthy case by construction: its expr is a PromQL filter, so 'no series' IS the healthy state (noDataState: OK). The exclusion set is derived in CI from gpu-switcher's WORKLOADS env var and every Rollout workloadRef with scaleDown: onsuccess, so it cannot drift silently. Rule carries health_bridge_only: 'true' — verified present on the live ConfigMap.<br>[frank:test_feature_health_workload_metrics.py](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_feature_health_workload_metrics.py>) |

## 25 · Agent shell retrieval store

| Acceptance | Origin | Levels | Automation | Evidence / notes |
| --- | --- | --- | --- | --- |
| From inside the hermes-agent-shell pod, an operator can reach a dedicated PostgreSQL 18 database with the pgvector extension enabled, on its own loopback port, without touching Hindsight. | [frank:2026-08-03--orch--hermes-retrieval-store-sidecar-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-08-03--orch--hermes-retrieval-store-sidecar-design.md>) | [UNIT](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_hermes_gbrain_sidecar.py>) ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live proof 2026-08-15 (post-merge, ArgoCD at 0fbbe549, pod hermes-agent-shell-859d56d94b-phjz6 4/4 Running): Test Plan row 2 — `select extversion from pg_extension where extname='vector'` inside the gbrain container returns 0.8.6. Row 3 — `df -h /opt/gbrain` shows its own Longhorn volume (pvc-a10dd99b, 9.8G) distinct from Hindsight's (pvc-cf287075, 4.9G). Reachability is genuinely loopback-only: the RUNNING server reports listen_addresses=127.0.0.1, port=5434 from pg_settings, not merely from the manifest. Hindsight untouched (row 1). Also confirmed the collation fix made during the 2026-08-15 review, which had a single first-initdb window: datlocprovider 'b', datlocale 'C.UTF-8'. NOT closed by the design-time container gate — that proved the image behaves under this posture on a laptop, which is evidence for the design, not for the deployment.<br>[frank:test_hermes_gbrain_sidecar.py](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_hermes_gbrain_sidecar.py>) |
| The retrieval store's data lives on its own backed-up Longhorn volume, so a Hindsight restore, migration or resize cannot take the retrieval store with it, and vice versa. | [frank:2026-08-03--orch--hermes-retrieval-store-sidecar-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-08-03--orch--hermes-retrieval-store-sidecar-design.md>) | [UNIT](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_hermes_gbrain_sidecar.py>) ~~API~~ ~~INT~~ ~~UI~~ | skipped | Live proof 2026-08-15 (post-merge). Row 1 — Hindsight's PVC still reports 5Gi and its container is ready with 0 restarts; gbrain is a separate 10Gi PVC. Row 3 — distinct Longhorn volumes (pvc-a10dd99b vs pvc-cf287075), distinct mounts. Row 5 — seeded a vector row + hnsw index, forced a CHECKPOINT, DELETED the pod; the replacement (…-qfssc, name changed, so genuinely recreated) came back 4/4 with the row still present and retrievable through an index scan, collation still builtin. The kubelet fsGroup re-walk left PGDATA at drwx------ 1000:1000 on the populated volume — the claim the local docker gate could only approximate, since it needs a real kubelet. Asserted on the artifacts, not on ArgoCD sync status: ArgoCD reported Synced/Healthy at the PRE-merge revision for ~2min after the merge, with a 3/3 pod, so sync status would have been a false positive here.<br>[frank:test_hermes_gbrain_sidecar.py](<https://github.com/derio-net/frank/blob/main/scripts/tests/test_hermes_gbrain_sidecar.py>) |
| An operator logging in over SSH has a JavaScript runtime available and can install a CLI from a git ref that survives pod restarts. | [frank:2026-08-03--orch--hermes-retrieval-store-sidecar-design.md](<https://github.com/derio-net/frank/blob/main/docs/superpowers/specs/2026-08-03--orch--hermes-retrieval-store-sidecar-design.md>) | ~~UNIT~~ ~~API~~ ~~INT~~ ~~UI~~ | not implemented | HALF PROVEN 2026-08-15, deliberately still open. Row 6 PASSES: `ssh hermes 'bash -lc "bun --version"'` returns 1.3.14 — a LOGIN shell, so the /etc/profile.d PATH shim is genuinely exercised (the live ssh pin is 4ed6ec9, which is past the c04eaab that introduced Bun; /usr/local/bin/bun is dated 2026-08-13). Rows 7-8 remain OWED and cannot be run from this repo: they need the CLI's private git ref, which lives in the requesting repo's private issue and is deliberately not written down here. Row 8 is the load-bearing one — it tests the PVC-persistence claim that decision 2 (CLI installed by hand rather than baked) entirely rests on. If it fails, decision 2 is wrong and the CLI belongs in the image. Do not close this row on row 6 alone: "a runtime exists" and "an installed CLI survives a restart" are different claims, and only the second decides the design. Rows 6-7 must be run in a LOGIN shell (bash -lc) — 'ssh host -- cmd' skips /etc/profile.d and would prove nothing. |

## § The sharp line — where drift can hide

### Skipped — verification exists but does not run in CI (backfill owed)

- **gpu1-usb-25g-node-ip-stable** — gpu-1 keeps serving the same Kubernetes node identity and 192.168.55.31 address through the replacement USB 2.5G Ethernet adapter.. _Live manual proof 2026-07-11 — ConfigPatch 404-gpu1-usb-25g-nic applied; gpu-1 Ready on 192.168.55.31 via enp0s20f0u7 MAC 6c:1f:f7:c6:e0:da._
- **gpu1-usb-25g-detected** — Talos detects the USB 2.5G Ethernet adapter with a usable driver, stable link state, and discoverable MAC address before the concrete network patch is applied.. _Live manual proof 2026-07-11 — Talos detected r8152 USB adapter enp0s20f0u7 with MAC 6c:1f:f7:c6:e0:da; missing rtl8156b firmware patch logged but link works._
- **gpu1-usb-25g-firmware-present** — gpu-1's Talos schematic ships the RTL8156B firmware (rtl_nic/rtl8156b-2.fw) for the USB 2.5G Ethernet adapter, so the r8152 driver loads it cleanly with no "Direct firmware load ... failed" warning at boot.. _Live manual proof 2026-07-16 — siderolabs/realtek-firmware (v20260309) applied to 402-gpu1-nvidia-extensions.yaml via omnictl; Omni recomputed schematic 54e23848, reinstalled + rebooted gpu-1 (~76s NotReady). Post-boot: extension present, ZERO rtl_nic firmware-load failures in dmesg (both rtl8156b USB and rtl8125b onboard cleared), gpu-1 Ready on 192.168.55.31 (USB NIC re-enumerated enp0s20f0u7->enp0s20f0u5, MAC-bound patch kept the address), nvidia allocatable 1, 19/19 pods back, cilium datapath OK._
- **gpu1-gpu-workloads-return** — After the NIC maintenance event, gpu-1 returns with NVIDIA allocatable GPU capacity and hard-pinned GPU/agent workloads recover to their intended state.. _Live manual proof 2026-07-11 — gpu-1 allocatable nvidia.com/gpu is 1; pinned workloads are Running or Completed after the network move._
- **net-all-hosts-declarative-dns** — Every Frank host has the internal DNS resolvers (192.168.10.11/12) set declaratively in its Talos machine config, so no host silently depends on DHCP-provided DNS.. _Live proof 2026-07-12: 102-cluster-nameservers applied via omnictl to all 7 Frank nodes (non-disruptive); talosctl get resolvers on gpu-1/mini-1/2/3/pc-1/raspi-1/2 all show 192.168.10.11 + 192.168.10.12 from the config layer. Unit guard scripts/tests/test_cluster_wide_nameservers.py passing (local guard, not CI-run)._
- **net-static-host-resolves-internal-dns** — A static-networked Frank host (e.g. gpu-1 on its USB adapter) resolves DNS via the internal resolvers and boots cleanly, with no fallback to ACL-blocked public DNS.. _Live proof 2026-07-12: gpu-1 (static USB-adapter host) rebooted via talosctl reboot — DOWN 16:51 -> Ready 16:53 (~2min, no time-sync boot-hang); resolvers 192.168.10.11/12 from the config layer; siderolink reconnected; GPU workloads rescheduled Running. The exact incident scenario now boots clean._
- **net-gpu1-single-declarative-ip** — gpu-1 presents a single declarative IPv4 address on its USB adapter (no DHCP-drift 192.168.55.150 alongside the static 192.168.55.31).. _Live proof 2026-07-12: after console-drift cleanup, talosctl -n 192.168.55.31 get addresses shows a single 192.168.55.31/24 on enp0s20f0u7 (no .150), and it persisted across the gpu-1 reboot-survival test._
- **obs-alert-triage-classifies** — The operator can invoke the frank-alert-triage skill and receive a per-alert verdict table classifying every firing Grafana alert (muted-canary, by-design, false-positive, known-tracker, or unexplained) without mutating the cluster.. _Live proof 2026-07-13: ran the frank-alert-triage skill against the live firing set — 2 cert-canaries muted, Layer 11 by-design (frank-ops#11), zero mutation; verdict table matched the manual triage. classify.py unit-tested across 8 decision-tree branches. github_issue is implemented as an orthogonal tracker annotation, not a benign known-tracker verdict, so a live NotReady pod still escalates._
- **obs-alert-agent-evidence-not-hn** — The alert-agent attributes a traffic surge from evidence (top referrers/user-agents) or states undetermined; it never defaults to a named source such as Hacker News.. _Live proof 2026-07-13: the Hacker News anchor was removed from SKILL.md and the orchestration.py surge/digest prompts (verified live in the rolled pod); a live surge reply attributed the traffic to a Chrome/120 stale-UA crawler from evidence, with no Hacker News. Prompt-content unit tests enforce the absence. (#631, #633)_
- **obs-alert-agent-plain-text-table** — Operator DM and digest replies from the alert-agent are compact plain-text tables, never a raw JSON envelope or a prose wall.. _Live proof 2026-07-13: drove session_send then render_payload end-to-end in the deployed pod — the agent's rich dict result renders as a compact plain-text table (aligned as_of/gpu_mode/alerts_firing/edge_traffic_level rows), raw-JSON=false. Fixed mechanically in render_payload (#634) after instruction-only attempts (#631 removed then #633 restored the text envelope) proved unable to override the session server's per-turn raw-JSON wrapper. Bridge unit tests cover the table render._
- **obs-alert-agent-answers-explicit-asks** — A focused explicit DM question to the alert-agent returns a real answer within the turn budget, not a timeout fallback.. _Live proof 2026-07-13: a focused DM turn returned status=ok within the bounded DM_TIMEOUT_S=150 (no timeout fallback). An overrun is mechanically backstopped by a deterministic frank-facts surge-compute snapshot, and a transport error can no longer silently drop the reply (#633). Bridge unit tests cover both the fallback and the exception path._
- **cnc-staging-secrets-in-vcluster** — The three CNC staging secrets (cnc-secrets, cnc-ghcr-pull, cnc-runner-auth) are present inside the cnc-staging vCluster with their expected names and keys, so cncd/node/fru can mount them.. _Live manual proof 2026-07-19 — all 3 secrets present inside the cnc-staging vCluster ns cnc-staging with correct keys (cnc-secrets/CNCD_SECRETS_MASTER_KEY, cnc-runner-auth/token, cnc-ghcr-pull/.dockerconfigjson) via fromHost.secrets after operator populated Infisical (prod env) + forced-sync/restart. No CI (host ESO + live vCluster required)._
- **cnc-staging-ghcr-pull-dockerconfigjson** — The cnc-ghcr-pull secret synced into the cnc-staging vCluster retains type kubernetes.io/dockerconfigjson, so CNC image pulls from ghcr succeed.. _Live manual proof 2026-07-19 — cnc-ghcr-pull synced into the cnc-staging vCluster (ns cnc-staging) RETAINED type kubernetes.io/dockerconfigjson (the primary risk); key .dockerconfigjson present. Verified via the vCluster kubeconfig. No CI (live vCluster required)._
- **cnc-prod-secret-delivery-unaffected** — CNC prod secret delivery (ExternalSecrets resolved directly in host namespace cnc) is unchanged by the staging secret-sync work.. _Statically guarded by scripts/tests/test_cnc_staging_host_secrets.py::test_prod_overlay_still_includes_the_three_externalsecrets (prod overlay still renders all 3 ExternalSecrets; cnc-base byte-unchanged — the staging change is a staging-scoped $patch:delete). Local guard (frank does not CI-run scripts/tests/); prod runtime path untouched by this PR._
- **stoa-mirror-all-private** — Every private agentic-stoa repo has a Gitea mirror on Frank whose main branch tracks GitHub automatically.. _Live proof 2026-07-20 — all 10 private repos on Gitea; second-brain/hermes-brain/flexible-health AND the cnc trio (discovered absent, never provisioned since June) created + sha-verified backfilled via scripts/tmp/phase5-gitea-actions.sh._
- **stoa-ci-runs-on-frank** — The Actions workflows of the five CI-bearing agentic-stoa repos execute on Frank via Gitea Actions, on PR sync branches, main pushes, and their schedules.. _Live proof 2026-07-20 — smoke PR second-brain#12 sync-pr-12; tests/unittest SUCCESS on act-runner; 14+ tasks executed across second-brain/hermes-brain/cnc-frd. Schedule-tick observation still pending._
- **stoa-ci-github-status-writeback** — A Frank-side CI run reports its result as a commit status on the originating GitHub PR sha, keeping branch protection meaningful.. _Live proof 2026-07-20 — gitea-actions/* statuses on GitHub sha 24f4291c (pending then success/failure transitions) via stoa-status-bridge, 6 PipelineRuns Succeeded. Required frank#664 (EL apply freeze) first._
- **report-tables-list-of-dicts** — A list-of-uniform-dicts payload renders as an aligned monospace column table in Telegram, not escaped one-line JSON.. _Unit-proven by render_report tests (aligned table, ragged-key union columns, first-seen order) over the captured live /edge_traffic fixture. Local guard — frank does not CI-run apps/*/tests; live-observed post-merge (report-tables-live-observed)._
- **report-tables-no-silent-truncation** — Over-cap lists show a +N more footer; no value is ever cut mid-character or mid-JSON.. _Unit-proven — 11-row fixture renders 10 rows + "+1 more", each shown value whole; scalar-list entries appear whole (the 200-char json-truncation defect is gone). Local guard (no CI run); live-observed post-merge._
- **report-tables-html-escape-safe** — Values containing angle brackets or ampersands deliver (never a silent HTML-400), proven by unit test and the 400-to-plain fallback path.. _Unit-proven — every value html.escaped (only <pre> tags are literal angle brackets); send_reply retries once as plain text on a canned 400, data preserved. Local guard (no CI run); the escape path is exercised end-to-end live post-merge._
- **report-tables-live-observed** — A live command turn in the deployed pod renders a real monospace table (operator-observed, post-merge).. _Live proof 2026-07-22 (PR 668 merged, ArgoCD synced fb6e41c, pod rolled onto bridge-cm with render_report/send_reply/_split_for_telegram present). Drove a realistic /edge_traffic payload through the DEPLOYED render_payload then send_reply, producing aligned monospace column tables, a ragged COUNTRY column with blank cells, a value with angle-brackets and ampersand HTML-escaped, sendMessage parse_mode=HTML. Fed the exact deployed code path (the agent turn itself hit the pre-existing DM latency ceiling, out of scope here). Local guard — no CI run._
- **cred-expiry-warns-before-expiry** — When the refresh token is 7 or fewer days from expiry, the daily check sends a Telegram warning whose urgency escalates at 3, 1, and expired.. _Unit-proven — evaluate_expiry tier boundaries (8→ok, 7→notice, 3→soon, 1→urgent, 0/-5→expired), should_warn iff tier!=ok, escalating plain-text wording; run_cred_check sends the warning via tg_send on threshold. Local guard (frank does not CI-run apps/*/tests); delivery observed live post-merge (cred-expiry-live-observed)._
- **cred-expiry-heartbeat-deadman** — A Grafana rule pages Telegram directly if the daily heartbeat line stops appearing in VictoriaLogs (checker died or pod down).. _Rule shape unit-pinned — VictoriaLogs stats query on the _msg field (NOT Hop's log field; verified live), lt 1 + noDataState OK, telegram_direct, 30h window, for 2h. Query validated server-side against live VictoriaLogs (returns 0 pre-deploy). Local guard; end-to-end firing observed live post-merge._
- **cred-expiry-robust-on-bad-cred** — A missing, unparseable, or field-less credential file yields a warning plus an error-tier heartbeat, never a silent skip.. _Unit-proven — missing/unparseable/field-less/bool/Infinity/NaN/out-of-range-epoch + non-FileNotFound read + invalid-UTF-8 all yield tier=error + a warning + a heartbeat; evaluate_expiry is total (never raises) and run_cred_check always heartbeats even on an unexpected error. Local guard; no CI run._
- **cred-expiry-live-observed** — Post-merge, both the warning and the dead-man heartbeat signals are triggered and observed live.. _Live proof 2026-07-22 (after PR 671 plus the PR 673 nested-field fix rolled). Heartbeat run against the REAL cred gave days_left=27 tier=ok; seeded to VictoriaLogs (dead-man query value=1). Forced 2-days-out cred via CRED_PATH delivered a real Telegram warning (tier=urgent). Grafana rule alert-agent-cred-expiry-heartbeat-stale loaded (provisioning API) and evaluates state=inactive/health=ok against the live heartbeat. Live-only._
- **agent-shell-talosctl-cluster-skew** — Operators driving Talos from an agent shell use a talosctl whose version is supported against the running cluster, instead of one three minor versions behind it.. _VERIFIED LIVE 2026-07-23 — secure-agent-pod kali container (image secure-agent-kali:c7a80f6) reports talosctl client Tag: v1.12.6, matching the cluster (all 7 nodes Talos v1.12.6). The three-minor skew (was v1.9.5, outside Talos's supported +/-1) is closed. The audit script's cluster anchor also resolves to v1.12.6 and marks the pin ok. Live manual proof; no CI level._
- **agent-shell-sweep-boots-clean** — Every agent shell Frank deploys still boots and serves after the upstream version sweep rebuilds it.. _VERIFIED LIVE 2026-07-23 — all rebuilt shells Running on c7a80f6: alert-agent (3 multi-agent-shell containers, s6-rc started clean, no crashloop-bail), n8n-01 (multi-agent-shell sidecar), secure-agent-pod (kali + vk-local 2/2), paperclip (paperclip-shell 2/2), ruflo (ruflo-server + ruflo-shell 2/2), hermes-agent-shell (hermes + hindsight + ssh 3/3). cnc-base NOT covered: its runner StatefulSet is not currently deployed (cnc-prod app OutOfSync/Missing, pre-existing — the manual-login persistent agent is down, unrelated to the sweep). Live manual proof._
- **hermes-upgrade-reaches-pod** — A Hermes version bump actually reaches the running shell, rather than shipping in the image while the pod keeps serving the old venv off its PVC.. _VERIFIED LIVE 2026-07-23, with a topology correction the live check surfaced. The DEPLOYED consumer of the bumped image is the hermes-agent-shell-ssh sidecar (built FROM docker v2026.7.20): its running container reports "Hermes Agent v0.19.0 (2026.7.20)" — the bump reached the pod. TWO nuances the spec's image->app mapping got wrong: (1) the base hermes-agent-shell image (PyPI 0.19.0, the .seed-version PVC-venv one this row was written about) is NOT deployed anywhere in frank — same status as infra-shell — so its bump is validated by CI smoke only; (2) the pod's runtime "hermes" container is docker.io/nousresearch/hermes-agent:v2026.7.7.2 (=0.18.2), pinned DIRECTLY in the manifest, NOT a ghcr.io/derio-net/* image, so the sweep did not touch it. Consequence: the intent_ack_continuation manual-op (frank plan P2.T2.S7) is moot for the current deployment — see the plan note. Live manual proof._
- **ruflo-revendor-preserves-function** — Ruflo still serves its UI and handles file upload and download after the upstream re-vendor, proving the local GridFS and wasm-scheme patches survived.. _VERIFIED LIVE 2026-07-23 — ruflo Running on ruflo-server:c7a80f6, readiness 1/1, /api/v2/feature-flags returns valid JSON. Both local patches confirmed present in the RUNNING build: the GridFS shim (Readable.from / new Writable) compiled into /app/build/server/chunks/database-*.js, and the wasm allow-line in urlSafety-*.js. The re-vendor (a6dd4ab->26c35b5) preserved both. package.json version 0.20.0. Live manual proof (endpoint + compiled-patch presence); a full authenticated file round-trip was not exercised, but the patch code is demonstrably in the artifact._
- **agent-images-pin-drift-measurable** — An operator can re-measure how far every agent-images pin has drifted from upstream on demand, without redoing the manual registry-by-registry reconnaissance.. _VERIFIED LIVE 2026-07-23 — scripts/version_audit.py (agent-images main c7a80f6) run against the live cluster reports every bumped pin ok, and its talosctl anchor resolves to the cluster's v1.12.6 (not GitHub latest v1.13.7), reading osImage "Talos (v1.12.6)". omnictl reports unknown (server version not discoverable from the repo — the honest-gap behaviour, by design). On-demand, no scheduled watcher (decision d3). Guarded by a test-scripts CI job (43 tests). Live manual proof._
- **fr-isolation-env-in-pods** — Agent processes in all four agent-bearing containers (secure-agent-pod kali+vk-local, hermes-agent-shell hermes+ssh) observe FR_ISOLATION_TARGET=worktree, enabling super-fr host-worktree isolation in-pod.. _Live manual proof 2026-07-24 post-PR#687 roll — kubectl exec in all four containers (kali, vk-local, hermes, ssh) printed FR_ISOLATION_TARGET=worktree. Guard test scripts/tests/test_fr_isolation_target_env.py pins the manifests in CI-less local runs._
- **fr-isolation-env-ssh-login** — Agents logging in over SSH/Mosh on either pod also see FR_ISOLATION_TARGET=worktree despite sshd env scrubbing (profile.d re-export shims).. _Live manual proof 2026-07-24 — env -i bash -lc printed worktree on kali (environ-loop shim, s6 PID 1, post-#687 roll) and on hermes ssh (static-export shim, PR#689 + rollout restart; /proc/1/environ is proctitle junk there since sshd is PID 1). Guard test pins both shims._
- **metrics-api-kubectl-top** — An operator can run 'kubectl top nodes' and 'kubectl top pods -A' and see live CPU/memory utilization for all 7 nodes (incl. both raspis) and pods across namespaces.. _Served by metrics-server (metrics.k8s.io). Live proof 2026-07-25 (PR #699 merged, root App-of-Apps synced to f530f0dd, metrics-server app Synced): kubectl top nodes returned all 7 nodes with live CPU/mem incl. both arm64 raspis (raspi-1 483m/12%/1663Mi, raspi-2 603m/15%/1861Mi, gpu-1 4785m/14%, mini-1/2/3, pc-1); kubectl top pods -A non-empty across namespaces; v1beta1.metrics.k8s.io APIService Available=True; metrics-server logs clean (no x509/scrape errors). Live manual proof — no CI level (frank does not run kubectl-top in CI)._
- **metrics-api-cpu-hpa** — A CPU/memory HorizontalPodAutoscaler can read live utilization (HPA TARGETS shows a real percentage, not <unknown>) and act on it.. _Live proof 2026-07-25: a throwaway CPU HPA on monitoring/blackbox-exporter (idle, 10m CPU request) reported TARGETS resolving from <unknown> to a real 40%/80% percentage once the HPA controller queried the Metrics API, proving resource metrics feed the HPA control loop; HPA then deleted (no standing HPA ships) and blackbox-exporter stayed 1/1 (no scale drift). Live manual proof — no CI level._
- **www-serves-versioned-page** — A visitor to https://www.derio.net receives the published site page over valid TLS, served from a versioned container image rather than a hard-coded Caddy string.. _Live proof 2026-07-26 - https://www.derio.net returns HTTP/2 200 over valid TLS serving the built page (867 bytes of Astro output, external stylesheet applied), from image ghcr.io/agentic-stoa/site:<sha> rather than the old 12-byte respond-literal. Image is a single-arch amd64/linux manifest matching hop-1; the tag in clusters/hop/apps/www was written by the site-promotion pipeline, not by hand. Test Plan step 1._
- **www-changes-reach-production** — A content change pushed to the private site repo reaches www.derio.net automatically, without an operator touching the Frank or Hop clusters.. _Live proof 2026-07-26 - observed end to end TWICE, the second time fully unattended. A page-content change pushed to the site repo main produced GitHub webhook 202, then agentic-stoa-main-sync, then the Gitea mirror at the same sha, then Gitea Actions green, then the GHCR image, then a site-promotion PipelineRun, then commit 74e9874c on frank main promoting the www image, then ArgoCD on Hop, then pod 1/1 on the new tag, then the changed page live. No operator touched either cluster. Required an UNPLANNED fix first - the Gitea push webhook to el-gitea-listener did not exist, so the promotion trigger had no delivery path (manual-op cicd-stoa-site-gitea-push-webhook). Test Plan step 6._
- **www-hardened-response-headers** — Both public sites (www.derio.net and blog.derio.net) return hardened response headers — HSTS, nosniff, Referrer-Policy, Permissions-Policy and a Content-Security-Policy — without breaking analytics.. _Live proof 2026-07-26 - both www.derio.net and blog.derio.net return HSTS (max-age=31536000, includeSubDomains, deliberately NO preload), nosniff, Referrer-Policy, Permissions-Policy and the CSP, with the Server header stripped. Analytics unbroken and the CSP proven ENFORCED rather than merely present - in a real browser the GoatCounter beacon fired on path / while an injected inline style element was blocked (style-src-elem violation), with zero violations from the page itself. Also verified on the handle_errors FALLBACK path, which originally served with all five headers missing and leaked the Server header - fixed in PR 705 and re-verified by scaling the backend to 0. Test Plan steps 2 and 3._
- **www-outage-is-visible** — If www.derio.net stops answering, the operator is alerted through the same Layer 17 edge path that already covers the blog.. _Live proof 2026-07-26 - probe_success for the www instance exists in the feature_health group and the layer-17-edge-down matcher returns exactly the two edge instances (blog and www), excluding the neighbouring frank.derio.net probes. The KNOWN LIMIT recorded when this row was written is now CLOSED by P6.T2.S4 - the probe uses the content-asserting www_content module, so with the backend scaled to 0 probe_success went from 1 to 0 within 20s, where the old status-only module would have stayed at 1 forever against the fallback 200. Recovered to 1 within 30s of scale-up. NOT held for the rule for:5m - layer-17-edge-down is severity:critical with github_issue frank-ops#17, so a deliberate fire would page and file a bug; rule coverage was proven independently by the matcher query. Test Plan step 5._
- **www-traffic-is-observable** — Requests to www.derio.net are searchable in central logging and counted in analytics, and abusive clients are blocked at the edge by CrowdSec.. _Live proof 2026-07-26 - VictoriaLogs returned 77 hits for the www.derio.net request host scoped to kubernetes.host hop-1 (the vhost new log directive); a real browser fired the GoatCounter beacon at counter.derio.net with path /; and CrowdSec caddy-logs cs_node_hits_ok_total rose by 11 across 10 requests to www, confirming the crowdsec directive parses the new vhost access log. Test Plan steps 3, 4 and 7._
- **kid-laptops-push-mirrors-to-gitea** — A push to derio-homelab/kid-laptops on GitHub reliably appears at the same commit SHA in the in-cluster Gitea mirror.. _Live proof 2026-07-27 — push to GitHub main, mirror PipelineRun kid-laptops-main-sync-z4gh6 Succeeded, and the Gitea mirror head matched GitHub at the identical SHA (5b895328edcf, later a72a3badc84a). Skipped rather than ci: the delivery path is forge webhook state with no IaC, so a webhook deleted by hand in either forge would break this silently. apps/tekton/webhooks.yaml declares the desired paths and scripts/tests/test_webhook_delivery_paths.py enforces the declaration at PR time, but nothing reconciles it against the live forges yet._
- **kid-laptops-mirror-push-creates-one-pipelinerun** — A push to the Gitea mirror creates exactly one kid-laptops PipelineRun in kid-laptops-ci, and no second run from the generic gitea-ci trigger.. _Live proof 2026-07-27 — five real pushes produced exactly one PipelineRun each in kid-laptops-ci and ZERO generic gitea-ci runs. The 'exactly one' half guarded a real defect: the generic gitea-push trigger's agentic-stoa-only exclusion would also have matched derio-homelab, adding a second failing run per push under a generic name. The manifest half IS enforced in frank CI by scripts/tests/test_kid_laptops_pipelinerun_contract.py (since #707); the live half needs a real push, hence skipped rather than ci._
- **kid-laptops-python-checks-run-per-push** — Every push to kid-laptops runs lint, ansible syntax-check and unit tests against the pushed commit, and the run is green when they pass.. _Live proof 2026-07-27 — PipelineRun kid-laptops-ci-fqs2h, 51s: clone, lint, syntax and unit-tests all Succeeded against the pushed commit (a72a3badc84a). Deliberately narrower than kid-laptops' own every-push-runs-ci-pipeline row: molecule-base and molecule-teardown are Skipped on the enable-molecule gate until KubeVirt lands, so this claims ONLY the Python/Ansible half. Reaching green needed two kid-laptops fixes CI itself surfaced (#48 taskRefs resolved from git, #50 vault shim covering ansible-lint). Not ci: the run is triggered by a push to kid-laptops, not by a frank PR, so a frank-side change cannot be caught here._
- **net-cluster-domain-canonical** — Operators can reach and authenticate to every Frank cluster service using its cluster.derio.net URL without depending on a legacy frank.derio.net route.. _VERIFIED LIVE 2026-08-03 — services remain reachable on *.cluster.derio.net now that the legacy frank fallback is gone. Spot-checked argocd.cluster.derio.net -> 200, with the *.cluster.derio.net wildcard intact for litellm/harbor/auth/grafana. The homelab master dashboard was repointed and redeployed: 15 tiles now target *.cluster.derio.net with live siteMonitor probes, omni keeps its preserved name, and the Portainer tile plus the rp.frank.derio.net Traefik bookmark are gone. CORRECTION worth recording: litellm.cluster.derio.net is the forward-auth admin UI and 302s an API client to SSO even with a valid Bearer key — so 'canonical' is per-consumer, not per-name. Machine clients use litellm-api.cluster.derio.net (no forward-auth, added in frank#749), verified returning LiteLLM's own 401 on /v1/models and resolving via the mesh resolver; kid-laptops and the operator .env files moved to it in derio-homelab/kid-laptops#81, with a tripwire against regression. BREADTH (2026-08-03, independent re-check): the row claims EVERY service, so the single-host spot-check above was widened to a ten-host sweep, all over valid TLS (curl ssl_verify_result=0) with the legacy fallback already NXDOMAIN and therefore unable to mask a failure — argocd and vk returned HTTP 200; grafana, longhorn, hubble, infisical, paperclip, auth, n8n and tekton returned HTTP 302 to Authentik, which is the correct forward-auth answer for a browserless client and, per the litellm correction above, is a pass for a human consumer rather than a machine one._
- **net-oidc-cutover-continuity** — Existing Kubernetes OIDC credentials continue to authenticate during the issuer migration while newly minted credentials use auth.cluster.derio.net.. _Live manual proof of the dual-issuer overlap. 2026-08-01: an old-issuer k8s-agent token retained from before the cutover kept authenticating under authenticator A while newly minted tokens carried iss=auth.cluster.derio.net, both normalizing to authentik:ak-Kubernetes Agent Access-client_credentials. 2026-08-02T08:51Z (T0+14h55m): re-proved by minting through both hosts — legacy iss=auth.frank.derio.net and cluster iss=auth.cluster.derio.net, both TokenReview authenticated=true with identical normalized identity and groups [system:authenticated], while the Omni service-account kubeconfig retained cluster-admin. Provider access_token_validity measured at hours=8, so every token issued at or before T0=2026-08-01T17:56:02Z expired by 2026-08-02T01:56:02Z. Not automatable in CI: needs a live cluster, the Authentik client secret and TokenReview._
- **net-legacy-domain-retired** — Every non-Omni frank.derio.net service name is removed from application configuration, ingress, and DNS after the migration overlap closes.. _VERIFIED LIVE 2026-08-03 — the DNS half this row was waiting on is done. derio-homelab/ansible-pihole#1 removed the frank.derio.net wildcard A (192.168.55.220); #2 removed the mini-1/2/3.frank.derio.net CNAMEs and the commented future-extension block after confirming nothing in any repo referenced them. Both deployed via dns.yaml. Measured AFTER deploy: 20 retired names x 3 resolvers = 60/60 NXDOMAIN — pihole A (192.168.10.11), pihole B (192.168.10.12), and the Headscale mesh resolver (100.100.100.100) from a mesh-joined host, which is the LAN-and-mesh proof this row demands and which scripts/tmp/p6-verify.sh explicitly cannot make. auth.frank.derio.net specifically stopped resolving to 192.168.55.220, closing the dangling default-cert name. The repository half stays CI-guarded by scripts/tests/test_frank_domain_retirement.py; the homelab side gained its own tripwire (ansible-homelab-automation/scripts/check-retired-domains.sh, wired into `make check`). NOTE: the first dns.yaml run applied a two-commit-stale checkout and exited 0 — only re-querying the names caught it, the same family as the omnictl-reads-a-local-file gotcha recorded elsewhere in this plan._
- **net-omni-domain-preserved** — Omni remains reachable and usable at omni.frank.derio.net after all other frank.derio.net service names are retired.. _VERIFIED LIVE 2026-08-03 — omni.frank.derio.net survived the wildcard removal because an explicit CNAME outlives the wildcard it sat under: it resolves to omni.frank.lan -> 192.168.10.30 on BOTH the LAN and the mesh resolver. Omni UI returns 200 with a genuine Let's Encrypt certificate (CN=omni.frank.derio.net, valid to 2026-10-08 — not a Traefik default cert); discovery returns 200; unauthenticated :8100 returns 401. Certificate renewal — which upstream required be preserved — is owned by proxmox-cluster's omni-cert-renew systemd timer rather than by Traefik, because Omni now runs on the gondor VM with no reverse proxy at all: the timer is active, its last run reported Result=success ExecMainStatus=0, and the next run is scheduled. p6-verify.sh: 37 pass / 0 fail / 0 pending._
- **litellm-reachable-from-mesh-by-name** — Frank publishes mesh-resolvable names for LiteLLM, and an authenticated API call to them returns a model list rather than being intercepted by the public SSO ingress.. _PROVEN LIVE 2026-08-02, after manual-op edge-headscale-litellm-mesh-dns-restart. DNS, read on a mesh node with all three controls so the homelab wildcard could not fake it: gitea-ssh.cluster.derio.net -> 192.168.55.209 (positive control: MagicDNS answering and outranking the wildcard), nope-xyz.cluster.derio.net -> 192.168.55.220 (negative control: the wildcard itself), litellm-lb.cluster.derio.net -> 192.168.55.206 (proof: differs from the wildcard, so it can only be the new map). Reachability: http://litellm-lb.cluster.derio.net:4000/health/liveliness -> 200. Route: https://litellm-api.cluster.derio.net/v1/models -> 401 from LiteLLM with a valid wildcard cert (ssl_verify=0), NOT a 302 to Authentik. Authenticated: the same URL with the vaulted kid-laptops virtual key -> 200 and a 12-model list (mistral-small-24b, gemma-12b, ...), which is the load-bearing check - every other result above is satisfied by a LiteLLM that rejects all keys. SSO regression: https://litellm.cluster.derio.net/ still 302s to Authentik. Status is skipped rather than ci because the proof needs a live mesh node and a real credential; the offline half (route shape, no forward-auth) IS ci-guarded under litellm-public-host-keeps-sso. The off-LAN foreign-network leg is derio-homelab/kid-laptops' row laptop-reaches-frank-from-anywhere._
- **infer-igpu-rerank-endpoint** — An in-cluster client can rerank a candidate passage set against a multilingual cross-encoder and receive scored, well-separated result indices.. _MET 2026-08-03 live: POST /v3/rerank returns {results:[{index,relevance_score}]}; scores well separated (0.0395 relevant vs 1.6e-5 irrelevant, ~2400x), not the degenerate 1e-9..1e-12 pattern. Both servables AVAILABLE via GET /v1/config. CI guards the manifest SHAPE (scripts/tests/test_ovms_retrieval_manifests.py), not the serving behaviour, so this row is live-proof rather than ci._
- **infer-igpu-rerank-latency-measured** — Operators have a measured rerank latency figure for a 20-passage batch on the mini iGPU, plus a same-node CPU control arm, instead of an arithmetic estimate.. _MET 2026-08-03 by live measurement (manual, not automatable in CI). iGPU rerank of 20 candidates: p50 77.3ms, p95 87.1ms, max 91.1ms, N=30 after warm-up. CPU control arm on the same node, same weights: p50 1612.9ms, p95 1702.6ms -> iGPU 20.9x faster. Caveat recorded: single-item embedding is only 1.1x (28.4 vs 32.2ms), request-overhead bound; and the ratio is at short sequence length._
- **infer-igpu-embeddings-endpoint** — In-cluster clients can obtain multilingual embeddings from Frank with a recorded vector dimensionality.. _MET 2026-08-03 live: POST /v3/embeddings returns 1024-dimensional vectors, dimension read from the response. Single p50 28.4ms, batch-32 p50 93.2ms, throughput 35.2/343.5 items per s. CI guards the manifest shape only; the serving behaviour is live proof._
- **infer-igpu-workload-preserves-quorum** — A warm inference workload pinned to a control-plane mini leaves etcd quorum and API-server latency unchanged relative to the two untouched peers.. _PARTIALLY MET 2026-08-03. 7500 rerank requests over 240s (~31/s) from mini-1: apiserver p99 (WATCH/CONNECT excluded) 23.4ms before vs 22.1ms under load; all three minis Ready, no Memory/DiskPressure; ovms working set 2254Mi of 6Gi. NOT VERIFIABLE: etcd leader changes and WAL fsync -- Frank does not scrape etcd (no etcd scrape job; the etcd_* series present are the apiserver's etcd CLIENT metrics). Tracked as a follow-up; the row is deliberately NOT marked fully met._
- **orch-hermes-retrieval-store-available** — From inside the hermes-agent-shell pod, an operator can reach a dedicated PostgreSQL 18 database with the pgvector extension enabled, on its own loopback port, without touching Hindsight.. _Live proof 2026-08-15 (post-merge, ArgoCD at 0fbbe549, pod hermes-agent-shell-859d56d94b-phjz6 4/4 Running): Test Plan row 2 — `select extversion from pg_extension where extname='vector'` inside the gbrain container returns 0.8.6. Row 3 — `df -h /opt/gbrain` shows its own Longhorn volume (pvc-a10dd99b, 9.8G) distinct from Hindsight's (pvc-cf287075, 4.9G). Reachability is genuinely loopback-only: the RUNNING server reports listen_addresses=127.0.0.1, port=5434 from pg_settings, not merely from the manifest. Hindsight untouched (row 1). Also confirmed the collation fix made during the 2026-08-15 review, which had a single first-initdb window: datlocprovider 'b', datlocale 'C.UTF-8'. NOT closed by the design-time container gate — that proved the image behaves under this posture on a laptop, which is evidence for the design, not for the deployment._
- **orch-hermes-retrieval-store-isolated** — The retrieval store's data lives on its own backed-up Longhorn volume, so a Hindsight restore, migration or resize cannot take the retrieval store with it, and vice versa.. _Live proof 2026-08-15 (post-merge). Row 1 — Hindsight's PVC still reports 5Gi and its container is ready with 0 restarts; gbrain is a separate 10Gi PVC. Row 3 — distinct Longhorn volumes (pvc-a10dd99b vs pvc-cf287075), distinct mounts. Row 5 — seeded a vector row + hnsw index, forced a CHECKPOINT, DELETED the pod; the replacement (…-qfssc, name changed, so genuinely recreated) came back 4/4 with the row still present and retrievable through an index scan, collation still builtin. The kubelet fsGroup re-walk left PGDATA at drwx------ 1000:1000 on the populated volume — the claim the local docker gate could only approximate, since it needs a real kubelet. Asserted on the artifacts, not on ArgoCD sync status: ArgoCD reported Synced/Healthy at the PRE-merge revision for ~2min after the merge, with a 3/3 pod, so sync status would have been a false positive here._

### Not implemented — no test or surface yet

- **gpu1-usb-25g-replacement-documented** — Operators have a documented, MAC-bound path to move gpu-1 from the damaged onboard Realtek NIC to the replacement USB 2.5G Ethernet adapter without changing node identity.. _Repo-prep row; verified by plan phase 1 docs/template validation._
- **gpu1-cilium-datapath-stable** — Cilium on gpu-1 initializes and keeps a healthy direct-routing datapath on the replacement NIC, with no layer-1 NIC flap alert breach during the soak.. _Manual post-swap Cilium verification plus 24h carrier-change soak._
- **hermes-dashboard-api-routable** — The Hermes web dashboard (hermes.cluster.derio.net) and gateway API (hermes-api.cluster.derio.net) are reachable through a Traefik IngressRoute gated by ip-allowlist + security-headers, with user authentication provided by the dashboard's own basic-auth (frank#626) rather than authentik forward-auth.. _2026-07-12 (willikins#285): authentik forward-auth removed from both hermes routes — no authentik application/proxy-provider was ever configured for these hosts, so the outpost 404'd every request. Mechanism is now the dashboard's own basic-auth (HERMES_DASHBOARD_BASIC_AUTH_*, frank#626; self-auth proven internally :9119/login -> 200). In-repo surface exists; becomes live-provable once this PR merges and ArgoCD reconciles the traefik-system IngressRoutes. SSO via authentik is a possible later follow-up._
- **stoa-ci-no-double-mutation** — While GitHub Actions and Frank CI run in parallel, mutating jobs (PR robots, tags, releases, issue upserts) execute from exactly one operator-selectable authority side.. _Guards merged in all 5 repos + CI_AUTHORITY=github org variable live (phase5 script). Stays open until the negative is observed — first Gitea-side pins-update tick skipping cleanly and no duplicate robot PRs during the August parallel week._
- **kid-laptops-ci-sa-can-manage-vmis** — The kid-laptops-ci-kubevirt ServiceAccount is authorised to create and delete VirtualMachineInstances in the kid-laptops-ci namespace.. _Deliberately still not-implemented. `kubectl auth can-i create virtualmachineinstances` answers yes (checked 2026-07-27) while the API server warns "the server doesn't have a resource type 'virtualmachineinstances'" — RBAC rules are opaque strings and are never resolved against installed CRDs, so that yes is true and proves nothing. Provable only once KubeVirt is deployed (spec 2026-07-26--virt--kubevirt-vms-design.md)._
- **frank-runs-virtual-machines** — An operator can run a virtual machine on Frank and reach its console.. _Design-only in this run; no KubeVirt on the cluster yet. Deliberately broad — it is the claim the whole virt layer exists to make._
- **feature-health-survives-node-reboot** — A control-plane rolling reboot completes without any feature-health alert firing for a pod terminated by the reboot.. _Phase 6 live run 2026-08-03 (PR #756, merge b8558529): 39 feature-health rules live, ZERO still querying kube_pod_status_ready, and the firing set fell 25 -> 3 (2 TLS canaries + 1 gpu_timeshare) — the exact muted/by-design baseline. STILL not-implemented because the claim is about what happens DURING a control-plane roll, and no roll has occurred since the merge. What is proven: the defect class is absent from the manifests (CI) and from the live ConfigMap. What is not: that a reboot passes quietly. Flips at the next node reboot._
- **feature-health-detects-real-outage** — A workload that loses all available replicas still raises its layer's feature-health alert.. _Phase 6 live run 2026-08-03: all 14 expressions return series against live VictoriaMetrics, each `or` branch independently, every series carrying workload + lowercase kind, every value 0 — the correct quiet state under gt 0 and the permanently-firing state under the old lt 1, which is the per-rule polarity proof. STILL not-implemented: that proves the queries resolve and are correctly polarised, not that losing replicas RAISES the alert. Needs an injected or genuine failure. Known narrowing recorded in grafana.md: layer-12 no longer watches the Rollout-managed sympozium-apiserver replicas._
- **feature-health-scale-to-zero-guarded** — An unexpectedly scaled-to-0 Deployment raises an alert, while the GPU-timeshared and Rollout-managed workloads that sit at 0 by design do not.. _Two-part claim, one part now proven live. PROVEN (2026-08-03, post-merge): the by-design workloads do NOT alert — comfyui (gpu-switcher timeshare) and litellm (Rollout scaleDown: onsuccess, serving 5/5) both sit at 0 replicas and the shipped expr returns 0 series, while the same query with exclusions stripped returns exactly those 2, so it is quiet for the right reason rather than by matching nothing. NOT PROVEN: that an UNEXPECTED scale-to-0 raises the alert — needs a real one. Note the rule sits in Normal (NoData) in the healthy case by construction: its expr is a PromQL filter, so 'no series' IS the healthy state (noDataState: OK). The exclusion set is derived in CI from gpu-switcher's WORKLOADS env var and every Rollout workloadRef with scaleDown: onsuccess, so it cannot drift silently. Rule carries health_bridge_only: 'true' — verified present on the live ConfigMap._
- **orch-hermes-shell-js-runtime** — An operator logging in over SSH has a JavaScript runtime available and can install a CLI from a git ref that survives pod restarts.. _HALF PROVEN 2026-08-15, deliberately still open. Row 6 PASSES: `ssh hermes 'bash -lc "bun --version"'` returns 1.3.14 — a LOGIN shell, so the /etc/profile.d PATH shim is genuinely exercised (the live ssh pin is 4ed6ec9, which is past the c04eaab that introduced Bun; /usr/local/bin/bun is dated 2026-08-13). Rows 7-8 remain OWED and cannot be run from this repo: they need the CLI's private git ref, which lives in the requesting repo's private issue and is deliberately not written down here. Row 8 is the load-bearing one — it tests the PVC-persistence claim that decision 2 (CLI installed by hand rather than baked) entirely rests on. If it fails, decision 2 is wrong and the CLI belongs in the image. Do not close this row on row 6 alone: "a runtime exists" and "an installed CLI survives a restart" are different claims, and only the second decides the design. Rows 6-7 must be run in a LOGIN shell (bash -lc) — 'ssh host -- cmd' skips /etc/profile.d and would prove nothing._

_Statuses: ci / scheduled cannot drift silently; skipped and not-implemented are CI warnings gated by the backfill rule; failing fails CI._
