schema_version: 2
phase:
  number: 0
  title: Manifests
  tag: agentic
  depends_on: []
  tracking_issue: https://github.com/derio-net/frank/issues/50
tasks:
- number: 1
  title: Add relay sidecar to vk-remote deployment
  steps:
  - id: P0.T1.S1
    text: |-
      Add relay-server container


      Add a second container to the pod spec, after the existing `vk-remote` container:

      ```yaml
              - name: relay-server
                image: ghcr.io/derio-net/vk-remote:<IMAGE_SHA>
                command: ["/usr/local/bin/relay-server"]
                ports:
                  - containerPort: 8082
                    protocol: TCP
                env:
                  - name: RELAY_LISTEN_ADDR
                    value: "0.0.0.0:8082"
                  - name: VIBEKANBAN_REMOTE_JWT_SECRET
                    valueFrom:
                      secretKeyRef:
                        name: vk-remote-secrets
                        key: VIBEKANBAN_REMOTE_JWT_SECRET
                  - name: POSTGRES_PASSWORD
                    valueFrom:
                      secretKeyRef:
                        name: vk-remote-secrets
                        key: POSTGRES_PASSWORD
                  - name: SERVER_DATABASE_URL
                    value: "postgresql://remote:$(POSTGRES_PASSWORD)@postgres-vk:5432/remote?sslmode=disable"
                resources:
                  requests:
                    cpu: 50m
                    memory: 64Mi
                  limits:
                    cpu: 200m
                    memory: 128Mi
                readinessProbe:
                  tcpSocket:
                    port: 8082
                  initialDelaySeconds: 5
                  periodSeconds: 15
                livenessProbe:
                  tcpSocket:
                    port: 8082
                  initialDelaySeconds: 10
                  periodSeconds: 30
      ```

      Use the same image tag as the vk-remote container. Replace `<IMAGE_SHA>` with the SHA from the vibe-kanban plan's Phase 0 Task 3.
  - id: P0.T1.S2
    text: |-
      Commit


      ```bash
      git add apps/vk-remote/manifests/deployment.yaml
      git commit -m "feat(agents): add relay-server sidecar to vk-remote pod"
      ```
- number: 2
  title: Create service with relay port
  steps:
  - id: P0.T2.S1
    text: |-
      Add relay port to the existing Service


      The Service definition is at the bottom of `deployment.yaml`. Add port 8082:

      ```yaml
      apiVersion: v1
      kind: Service
      metadata:
        name: vk-remote
        namespace: agents
      spec:
        selector:
          app: vk-remote
        ports:
          - name: http
            port: 8081
            targetPort: 8081
          - name: relay
            port: 8082
            targetPort: 8082
      ```
  - id: P0.T2.S2
    text: |-
      Commit


      ```bash
      git add apps/vk-remote/manifests/deployment.yaml
      git commit -m "feat(agents): add relay port to vk-remote service"
      ```
- number: 3
  title: Split IngressRoute for relay paths
  steps:
  - id: P0.T3.S1
    text: |-
      Replace the existing vk-remote IngressRoute with two rules


      Find the existing VK IngressRoute block:

      ```yaml
          - match: Host(`vk.cluster.derio.net`)
            kind: Rule
            middlewares:
              - name: ip-allowlist
              - name: security-headers
            services:
              - name: vk-remote
                namespace: agents
                port: 8081
      ```

      Replace with two rules:

      ```yaml
          - match: Host(`vk.cluster.derio.net`) && PathPrefix(`/v1/relay`)
            kind: Rule
            middlewares:
              - name: ip-allowlist
              - name: security-headers
            services:
              - name: vk-remote
                namespace: agents
                port: 8082
          - match: Host(`vk.cluster.derio.net`)
            kind: Rule
            middlewares:
              - name: ip-allowlist
              - name: security-headers
            services:
              - name: vk-remote
                namespace: agents
                port: 8081
      ```

      The relay rule must come first — Traefik evaluates rules in order and the more specific PathPrefix match needs priority.
  - id: P0.T3.S2
    text: |-
      Commit


      ```bash
      git add apps/traefik/manifests/ingressroutes.yaml
      git commit -m "feat(agents): split vk IngressRoute for relay paths"
      ```
- number: 4
  title: Add VK_SHARED_RELAY_API_BASE to secure-agent-pod
  steps:
  - id: P0.T4.S1
    text: |-
      Add the env var


      In the kali container's `env` section, after `VK_SHARED_API_BASE`, add:

      ```yaml
                  - name: VK_SHARED_RELAY_API_BASE
                    value: "https://vk.cluster.derio.net"
      ```
  - id: P0.T4.S2
    text: |-
      Commit


      ```bash
      git add apps/secure-agent-pod/manifests/deployment.yaml
      git commit -m "feat(agents): add VK_SHARED_RELAY_API_BASE for relay connection"
      ```
- number: 5
  title: Commit all and push
  steps:
  - id: P0.T5.S1
    text: |-
      Push to trigger ArgoCD sync


      ```bash
      git push origin main
      ```

      ---
state:
  steps:
    P0.T1.S1:
      state: x
      ticked_at: null
      note: null
    P0.T1.S2:
      state: x
      ticked_at: null
      note: null
    P0.T2.S1:
      state: x
      ticked_at: null
      note: null
    P0.T2.S2:
      state: x
      ticked_at: null
      note: null
    P0.T3.S1:
      state: x
      ticked_at: null
      note: null
    P0.T3.S2:
      state: x
      ticked_at: null
      note: null
    P0.T4.S1:
      state: x
      ticked_at: null
      note: null
    P0.T4.S2:
      state: x
      ticked_at: null
      note: null
    P0.T5.S1:
      state: x
      ticked_at: null
      note: null
  completion:
    at: null
    note: null
    observed_prs: []
