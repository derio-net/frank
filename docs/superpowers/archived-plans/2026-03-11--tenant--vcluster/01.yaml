schema_version: 2
phase:
  number: 1
  title: Multi-tenancy — vCluster Implementation Plan
  tag: agentic
  depends_on: []
  tracking_issue: null
tasks:
- number: 1
  title: Create the vCluster template values file
  steps:
  - id: P1.T1.S1
    text: |-
      Create directory structure


      ```bash
      mkdir -p apps/vclusters/template
      ```
  - id: P1.T1.S2
    text: |-
      Write the template values file


      Create `apps/vclusters/template/values.yaml`:

      ```yaml
      # vCluster template values — sensible defaults for Frank cluster
      # Individual vClusters import these and override as needed.
      #
      # Chart: loft-sh/vcluster v0.32.1
      # Repo:  https://charts.loft.sh

      # Syncer configuration — what gets synced between host and virtual cluster
      sync:
        toHost:
          pods:
            enabled: true
          services:
            enabled: true
          configmaps:
            enabled: true
          secrets:
            enabled: true
          endpoints:
            enabled: true
          persistentvolumeclaims:
            enabled: true
          ingresses:
            enabled: true
        fromHost:
          nodes:
            enabled: true
          storageClasses:
            enabled: true

      # Backing store — embedded etcd with Longhorn persistence
      controlPlane:
        backingStore:
          etcd:
            embedded:
              enabled: true
        statefulSet:
          persistence:
            volumeClaim:
              enabled: true
              storageClass: longhorn
              size: 5Gi
              retentionPolicy: Delete
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi

      # Networking — no built-in ingress, access via vcluster connect
      networking:
        service:
          type: ClusterIP

      # Isolation — deny host cluster access from tenant workloads
      isolation:
        enabled: true
        podSecurityStandard: baseline
        resourceQuota:
          enabled: true
          quota:
            requests.cpu: "4"
            requests.memory: 8Gi
            limits.cpu: "8"
            limits.memory: 16Gi
            pods: "50"
            services: "20"
            persistentvolumeclaims: "10"
        limitRange:
          enabled: true
          default:
            cpu: 500m
            memory: 512Mi
          defaultRequest:
            cpu: 100m
            memory: 128Mi
        networkPolicy:
          enabled: true
      ```
  - id: P1.T1.S3
    text: |-
      Commit


      ```bash
      git add apps/vclusters/template/values.yaml
      git commit -m "feat(phase12): add vCluster template values

      Sensible defaults for disposable virtual clusters:
      - Embedded etcd on Longhorn (5Gi)
      - Baseline pod security
      - Resource quotas (4 CPU / 8Gi request, 50 pods)
      - Network policies enabled"
      ```

      ---
- number: 2
  title: Create the "experiments" vCluster namespace
  steps:
  - id: P1.T2.S1
    text: |-
      Write namespace manifest


      Create `apps/root/templates/ns-vcluster-experiments.yaml`:

      ```yaml
      apiVersion: v1
      kind: Namespace
      metadata:
        name: vcluster-experiments
        labels:
          pod-security.kubernetes.io/enforce: baseline
          pod-security.kubernetes.io/enforce-version: latest
          pod-security.kubernetes.io/audit: restricted
          pod-security.kubernetes.io/warn: restricted
      ```
  - id: P1.T2.S2
    text: |-
      Commit


      ```bash
      git add apps/root/templates/ns-vcluster-experiments.yaml
      git commit -m "feat(phase12): add vcluster-experiments namespace

      Baseline pod security enforcement, restricted audit/warn."
      ```

      ---
- number: 3
  title: Create the "experiments" vCluster values (overrides only)
  steps:
  - id: P1.T3.S1
    text: |-
      Create directory


      ```bash
      mkdir -p apps/vclusters/experiments
      ```
  - id: P1.T3.S2
    text: |-
      Write values file (overrides only)


      Create `apps/vclusters/experiments/values.yaml`:

      ```yaml
      # vCluster "experiments" — disposable sandbox environment
      # Base values come from apps/vclusters/template/values.yaml (loaded first in ArgoCD).
      # This file contains only instance-specific overrides.
      #
      # Chart: loft-sh/vcluster v0.32.1
      # Namespace: vcluster-experiments

      # Currently using all template defaults.
      # To override, add only the keys that differ. Examples:
      #
      # controlPlane:
      #   statefulSet:
      #     persistence:
      #       volumeClaim:
      #         size: 10Gi      # Larger etcd for long-lived experiments
      #
      # isolation:
      #   resourceQuota:
      #     quota:
      #       pods: "100"       # Allow more pods in this vCluster
      ```
  - id: P1.T3.S3
    text: |-
      Commit


      ```bash
      git add apps/vclusters/experiments/values.yaml
      git commit -m "feat(phase12): add experiments vCluster override values

      First concrete vCluster instance — sandbox for experiments.
      Inherits all defaults from template/values.yaml."
      ```

      ---
- number: 4
  title: Create the ArgoCD Application CR for "experiments"
  steps:
  - id: P1.T4.S1
    text: |-
      Write the Application CR


      Create `apps/root/templates/vcluster-experiments.yaml`:

      ```yaml
      apiVersion: argoproj.io/v1alpha1
      kind: Application
      metadata:
        name: vcluster-experiments
        namespace: argocd
        finalizers:
          - resources-finalizer.argocd.argoproj.io
      spec:
        project: infrastructure
        sources:
          - repoURL: https://charts.loft.sh
            chart: vcluster
            targetRevision: "0.32.1"
            helm:
              releaseName: experiments
              valueFiles:
                # Template first (base defaults), then instance overrides
                - $values/apps/vclusters/template/values.yaml
                - $values/apps/vclusters/experiments/values.yaml
          - repoURL: {{ .Values.repoURL }}
            targetRevision: {{ .Values.targetRevision }}
            ref: values
        destination:
          server: {{ .Values.destination.server }}
          namespace: vcluster-experiments
        syncPolicy:
          automated:
            prune: false
            selfHeal: true
          syncOptions:
            - CreateNamespace=true
            - ServerSideApply=true
            - RespectIgnoreDifferences=true
        ignoreDifferences:
          - group: ""
            kind: Secret
            jsonPointers:
              - /data
      ```
  - id: P1.T4.S2
    text: |-
      Commit


      ```bash
      git add apps/root/templates/vcluster-experiments.yaml
      git commit -m "feat(phase12): add ArgoCD app for experiments vCluster

      Deploys loft-sh/vcluster 0.32.1 into vcluster-experiments namespace.
      ServerSideApply + selfHeal, Secret data ignored."
      ```

      ---
- number: 5
  title: Push and verify ArgoCD sync
  steps:
  - id: P1.T5.S1
    text: |-
      Push all commits


      ```bash
      git push origin main
      ```
  - id: P1.T5.S2
    text: |-
      Wait for ArgoCD to pick up changes


      ```bash
      # Check root app sees the new application
      argocd app list --port-forward --port-forward-namespace argocd | grep vcluster
      ```

      Expected: `vcluster-experiments` appears in the app list.
  - id: P1.T5.S3
    text: |-
      Sync if not auto-synced


      ```bash
      argocd app sync root --port-forward --port-forward-namespace argocd
      ```
  - id: P1.T5.S4
    text: |-
      Wait for vCluster to become healthy


      ```bash
      # Watch the vCluster pods come up
      kubectl get pods -n vcluster-experiments -w
      ```

      Expected: `experiments-0` pod reaches `Running` + `Ready` state (may take 1-2 minutes for etcd init).
  - id: P1.T5.S5
    text: |-
      Verify ArgoCD app health


      ```bash
      argocd app get vcluster-experiments --port-forward --port-forward-namespace argocd
      ```

      Expected: Status `Synced`, Health `Healthy`.
  - id: P1.T5.S6
    text: |-
      Commit nothing — this is a verification step only


      ---
- number: 6
  title: Connect to vCluster and validate
  steps:
  - id: P1.T6.S1
    text: |-
      Install vcluster CLI (if not present)


      ```bash
      if ! command -v vcluster &> /dev/null; then
        curl -L -o vcluster "https://github.com/loft-sh/vcluster/releases/download/v0.32.1/vcluster-linux-amd64"
        chmod +x vcluster
        sudo mv vcluster /usr/local/bin/
        vcluster --version
      else
        echo "vcluster CLI already installed: $(vcluster --version)"
      fi
      ```

      Expected: `vcluster version 0.32.1` (or similar).
  - id: P1.T6.S2
    text: |-
      Connect to the experiments vCluster


      ```bash
      vcluster connect experiments -n vcluster-experiments
      ```

      Expected: A kubeconfig is generated and the current context is switched to the virtual cluster.
  - id: P1.T6.S3
    text: |-
      Verify the virtual cluster is functional


      ```bash
      # Inside the vCluster context:
      kubectl get nodes
      kubectl get namespaces
      kubectl create namespace test-sandbox
      kubectl run nginx --image=nginx:alpine -n test-sandbox
      kubectl get pods -n test-sandbox -w
      ```

      Expected: Nodes are visible (synced from host), nginx pod reaches `Running`.
  - id: P1.T6.S4
    text: |-
      Verify host-side isolation


      ```bash
      # Switch back to host context
      vcluster disconnect

      # The nginx pod should appear in the host namespace (synced)
      kubectl get pods -n vcluster-experiments | grep nginx

      # But the pod should NOT be visible in other namespaces
      kubectl get pods -A | grep test-sandbox
      # Expected: no results (virtual namespace doesn't exist on host)
      ```
  - id: P1.T6.S5
    text: |-
      Clean up test workload


      ```bash
      vcluster connect experiments -n vcluster-experiments
      kubectl delete namespace test-sandbox
      vcluster disconnect
      ```
  - id: P1.T6.S6
    text: |-
      Commit nothing — verification only


      ---

      ## Chunk 2: Documentation and Design File Update
- number: 7
  title: Rename design file (COMPLETED — already renamed to layer-based naming)
  steps:
  - id: P1.T7.S1
    text: |-
      The design file has been renamed to `docs/superpowers/specs/2026-03-07--tenant--vcluster-design.md` as part of the layer naming convention update.

      ---
- number: 8
  title: Update CLAUDE.md services table
  steps:
  - id: P1.T8.S1
    text: |-
      Update Architecture directory tree in CLAUDE.md


      vCluster access is kubeconfig-based (`vcluster connect`), not exposed via a LoadBalancer UI. No entry needed in the Services table.

      In the `## Architecture` section's directory tree, add the `vclusters/` entry under `apps/`. Find the line `<app>/manifests/     # Raw K8s manifests (when no upstream chart)` and add after it:

      ```
        vclusters/             # Per-vCluster Helm values (multi-tenancy)
          template/            # Base values template
          <name>/values.yaml   # Per-instance overrides
      ```
  - id: P1.T8.S2
    text: |-
      Commit


      ```bash
      git add CLAUDE.md
      git commit -m "docs: add vclusters directory to CLAUDE.md architecture

      Documents the per-vCluster values pattern under apps/."
      ```

      ---
- number: 9
  title: Follow standard layer workflow — Blog, README, Runbook
  steps:
  - id: P1.T9.S1
    text: |-
      Blog post — Run `/blog-post` skill. Title: "Multi-tenancy: Disposable Kubernetes Clusters with vCluster". Update the series index (`blog/content/posts/00-overview/index.md`) and roadmap shortcode (`blog/layouts/shortcodes/cluster-roadmap.html`).
  - id: P1.T9.S2
    text: |-
      Update README — Run `/update-readme` to sync Technology Stack, Repository Structure, Service Access, and Current Status.
  - id: P1.T9.S3
    text: |-
      Sync runbook — Run `/sync-runbook` if any `# manual-operation` blocks exist in this plan. (This plan has none — vCluster CLI install is a developer tool, not a cluster operation.)
  - id: P1.T9.S4
    text: |-
      Review — Verify deployment health and blog accuracy.
state:
  steps:
    P1.T1.S1:
      state: x
      ticked_at: null
      note: null
    P1.T1.S2:
      state: x
      ticked_at: null
      note: null
    P1.T1.S3:
      state: x
      ticked_at: null
      note: null
    P1.T2.S1:
      state: x
      ticked_at: null
      note: null
    P1.T2.S2:
      state: x
      ticked_at: null
      note: null
    P1.T3.S1:
      state: x
      ticked_at: null
      note: null
    P1.T3.S2:
      state: x
      ticked_at: null
      note: null
    P1.T3.S3:
      state: x
      ticked_at: null
      note: null
    P1.T4.S1:
      state: x
      ticked_at: null
      note: null
    P1.T4.S2:
      state: x
      ticked_at: null
      note: null
    P1.T5.S1:
      state: x
      ticked_at: null
      note: null
    P1.T5.S2:
      state: x
      ticked_at: null
      note: null
    P1.T5.S3:
      state: x
      ticked_at: null
      note: null
    P1.T5.S4:
      state: x
      ticked_at: null
      note: null
    P1.T5.S5:
      state: x
      ticked_at: null
      note: null
    P1.T5.S6:
      state: x
      ticked_at: null
      note: null
    P1.T6.S1:
      state: x
      ticked_at: null
      note: null
    P1.T6.S2:
      state: x
      ticked_at: null
      note: null
    P1.T6.S3:
      state: x
      ticked_at: null
      note: null
    P1.T6.S4:
      state: x
      ticked_at: null
      note: null
    P1.T6.S5:
      state: x
      ticked_at: null
      note: null
    P1.T6.S6:
      state: x
      ticked_at: null
      note: null
    P1.T7.S1:
      state: ' '
      ticked_at: null
      note: null
    P1.T8.S1:
      state: x
      ticked_at: null
      note: null
    P1.T8.S2:
      state: x
      ticked_at: null
      note: null
    P1.T9.S1:
      state: x
      ticked_at: null
      note: null
    P1.T9.S2:
      state: x
      ticked_at: null
      note: null
    P1.T9.S3:
      state: x
      ticked_at: null
      note: null
    P1.T9.S4:
      state: x
      ticked_at: null
      note: null
  completion:
    at: null
    note: null
    observed_prs: []
