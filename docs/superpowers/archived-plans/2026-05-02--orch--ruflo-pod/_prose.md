# Ruflo Pod Implementation Plan

## Phase 1: Build `ruflo-server` and `ruflo-shell` images (agent-images repo)

### Task 1: Investigate base image inheritance (shared with paperclip-shell)

- P1.T1.S1: Read `agent-shell-base` Dockerfile and rootfs layout

- P1.T1.S2: Confirm s6-overlay non-root-mode fixes have landed

- P1.T1.S3: Read `paperclip-shell` rootfs to compare against

### Task 2: Investigate upstream ruvocal Dockerfile

- P1.T2.S1: Clone upstream ruvnet/ruflo at HEAD and read the ruvocal Dockerfile

- P1.T2.S2: Decide build path — record the decision in this plan's Deployment Notes:

### Task 3: Add `ruflo-server/` directory

- P1.T3.S1: Create directory layout

- P1.T3.S2: Write the Dockerfile (use BEGIN/END markers because the file pins an upstream SHA inline)

- P1.T3.S3: Write `ruflo-server/README.md`

### Task 4: Add `ruflo-shell/` directory (clone of `paperclip-shell` with three diffs)

- P1.T4.S1: Copy `paperclip-shell/` to `ruflo-shell/` as a starting point

- P1.T4.S2: Adjust `ruflo-shell/Dockerfile` — add `@anthropic-ai/claude-code` to Layer-1 baked-in tools (the operator wants `claude` available immediately from the shell, before the inventory has been populated)

- P1.T4.S3: Adjust `/etc/skel/.bashrc` to export `LITELLM_BASE_URL` and the operator-facing banner

- P1.T4.S4: Verify `notify-telegram.sh` references `ruflo-shell` in its alert title and `kubectl logs` hint (the sed in Task 4 Step 1 should have caught this; confirm by reading the file).

- P1.T4.S5: Update `ruflo-shell/README.md` — same shape as paperclip-shell's README but pointing at this plan and noting the `claude-flow` is *not* baked in (it lives in the inventory, where it can be bumped without rebuilding the image).

### Task 5: Add CI matrix entries + smoke tests

- P1.T5.S1: Add `ruflo-server` to the build matrix in `.github/workflows/build.yaml` (or whatever the agent-images CI workflow is named) alongside `secure-agent-kali`, `vk-local`, and `paperclip-shell`. Build pushes to `ghcr.io/derio-net/ruflo-server:<sha>`.

- P1.T5.S2: Add `ruflo-shell` to the same build matrix. Build pushes to `ghcr.io/derio-net/ruflo-shell:<sha>`.

- P1.T5.S3: Add smoke test for `ruflo-shell` mirroring paperclip-shell:

- P1.T5.S4: Add smoke test for `ruflo-server` — boot the container against a transient mongo and curl the healthcheck:

### Task 6: Open PR, review, merge

- P1.T6.S1: Open PR titled `feat: add ruflo-server and ruflo-shell images` with body linking to this plan. Wait for CI green on both images' smoke tests.

- P1.T6.S2: Capture both merged image SHAs — record in this plan's *Deployment Notes* as:

## Phase 2: Frank manifests for ruflo + ruflo-db

### Task 1: Pre-flight Infisical

- P2.T1.S1: Generate `RUFLO_DB_PASSWORD` (32+ char random) and add to Infisical under the path the `apps/ruflo/manifests/externalsecret-db-credentials.yaml` ESO will reference. <!-- Manual op — operator action before this PR can deploy. Tracked in Deployment Notes. -->

- P2.T1.S2: Confirm `OPENROUTER_API_KEY` and `EMAIL_RESEND_API_KEY` are accessible from a `ruflo-system`-permitted Infisical path. If not, copy the entries under `/shared` (the path used by paperclip's ESOs) or extend ServiceAccount access. <!-- Manual op — operator action before this PR can deploy. Tracked in Deployment Notes. -->

- P2.T1.S1: Provision the virtual key via the LiteLLM admin API and store the returned secret as `RUFLO_LITELLM_KEY` in Infisical. *(Done 2026-05-03 — confirmed via `OPENAI_API_KEY` prefix `sk-cEqfu9e8…` in the running pod; LiteLLM `/models` returns 200.)*

### Task 2: Add `apps/ruflo-db/` ArgoCD sub-app

- P2.T2.S1: Read `apps/paperclip-db/` to mirror its shape

- P2.T2.S2: Create `apps/ruflo-db/values.yaml` — copy paperclip-db's values, adjust naming (`paperclip` → `ruflo`), set PVC size to `20Gi`, set the secret reference to a new ESO-managed Secret name (`ruflo-db-credentials`).

- P2.T2.S3: Create `apps/ruflo-db/manifests/externalsecret-db.yaml` — ESO producing `ruflo-db-credentials` Secret in namespace `ruflo-system`, sourcing `RUFLO_DB_PASSWORD` from Infisical. Use `apps/paperclip-db/manifests/external-secret-*.yaml` (or paperclip's equivalent) as the template. Ensure the produced Secret also exposes the full `MONGO_URL` connection string (including the password) so ruvocal can consume it directly via `secretKeyRef`. <!-- Deviated: upstream ruvocal switched MongoDB→PostgreSQL (P1 finding). DB password is chart-generated by Bitnami postgresql Helm chart; no Infisical ESO needed. Deployment reads from auto-generated 'ruflo-db-postgresql' Secret and assembles DATABASE_URL inline (paperclip pattern). -->

- P2.T2.S4: Create `apps/root/templates/ruflo-db.yaml` — Application CR mirroring `apps/root/templates/paperclip-db.yaml`. Same `syncPolicy`: `ServerSideApply=true`, `prune: false`, `selfHeal: true`. Sync wave should be **before** `ruflo` so Mongo is up when ruvocal connects on first boot.

### Task 3: Add `apps/ruflo/` namespace, PVCs, ConfigMap, ServiceAccount

- P2.T3.S1: Create `apps/ruflo/manifests/namespace.yaml`

- P2.T3.S2: Create `apps/ruflo/manifests/pvc-workspace.yaml`

- P2.T3.S3: Create `apps/ruflo/manifests/pvc-shell-home.yaml`

- P2.T3.S4: Create `apps/ruflo/manifests/configmap-shell-inventory.yaml` (initially empty arrays, populated in Phase 4)

- P2.T3.S5: Create `apps/ruflo/manifests/serviceaccount.yaml` — minimal SA for the pod and for ESO.

### Task 4: Add ExternalSecrets

- P2.T4.S1: Read existing reference ESOs

- P2.T4.S2: Create `apps/ruflo/manifests/externalsecret-openrouter.yaml` producing Secret `ruflo-openrouter` with key `OPENROUTER_API_KEY` from Infisical.

- P2.T4.S3: Create `apps/ruflo/manifests/externalsecret-resend.yaml` producing Secret `ruflo-resend` with key `EMAIL_RESEND_API_KEY` from Infisical. Use the paperclip resend ESO as the literal template.

- P2.T4.S4: Create `apps/ruflo/manifests/externalsecret-shell-ssh-keys.yaml` producing Secret `ruflo-shell-ssh-keys` from the same Infisical entries that `agent-ssh-keys` uses (shared with secure-agent-pod and paperclip-shell). <!-- Deviated: agent-ssh-keys (the analogous secret on secure-agent-pod) is SOPS-bootstrap, not Infisical-ESO. ruflo-shell-ssh-keys mirrors that pattern — see secrets/ruflo/README.md. Deployment volume marked optional:true so pod boots even before bootstrap. The bootstrap/rotation flow is captured below as a manual-operation block. -->

- P2.T4.S5: Create `apps/ruflo/manifests/externalsecret-shell-alerts.yaml` producing Secret `ruflo-shell-alerts` with `FRANK_C2_TELEGRAM_BOT_TOKEN` and `FRANK_C2_TELEGRAM_CHAT_ID`. Mirror paperclip-shell's equivalent (which Phase 2 of that plan introduces — copy from there if it has merged by the time this phase runs; otherwise create from scratch using the same Infisical source).

### Task 5: Add the Web UI ClusterIP Service

- P2.T5.S1: Create `apps/ruflo/manifests/service-web.yaml`

### Task 6: Add the Shell SSH+Mosh LoadBalancer Service

- P2.T6.S1: Confirm `192.168.55.222` is free

- P2.T6.S2: Create `apps/ruflo/manifests/service-shell.yaml`

### Task 7: Write the Deployment with both containers

- P2.T7.S1: Investigate the ruvocal container's expected workspace path (informational; locks the volumeMount paths)

- P2.T7.S2: Create `apps/ruflo/manifests/deployment.yaml` with both containers

### Task 8: Add Application CRs to `apps/root/templates/`

- P2.T8.S1: Create `apps/root/templates/ruflo-db.yaml` — Application CR mirroring paperclip-db's; sync wave ordered before ruflo.

- P2.T8.S2: Create `apps/root/templates/ruflo.yaml` — Application CR mirroring paperclip's. Source path `apps/ruflo/manifests`, destination namespace `ruflo-system`. Include `ServerSideApply=true`, `prune: false`, `selfHeal: true`, and `ignoreDifferences` on Secret `/data` jsonPointer (per the standard pattern in `frank-argocd.md`).

### Task 9: Wire web UI to Traefik + Authentik

- P2.T9.S1: Add IngressRoute to `apps/traefik/manifests/ingressroutes.yaml`

- P2.T9.S2: Add proxy provider entry to `apps/authentik-extras/manifests/blueprints-cluster-proxy-providers.yaml`

### Task 10: Add the homepage tile

- P2.T10.S1: Edit `apps/homepage/manifests/configmap-services.yaml`

### Task 11: Add operator client-setup files

- P2.T11.S1: Create `apps/ruflo/client-setup/laptop/` mirroring `apps/paperclip/client-setup/laptop/` (or `apps/secure-agent-pod/client-setup/laptop/`):

### Task 12: Open frank PR

- P2.T12.S1: Open PR titled `feat(orch): add ruflo pod (manifests, ruflo-db sub-app, ingress, Authentik)` with body linking to this plan and the spec. Note: the inventory ConfigMap is empty — Phase 4 populates it; the Authentik outpost provider assignment is a manual op in Phase 3.

- P2.T12.S2: Wait for ArgoCD auto-sync after merge

## Phase 3: First-deploy validation

### Task 1: ruflo-db health

- P3.T1.S1: ruvocal data PVC Bound, mount writable to UID 1000 *(was: "Mongo pod Ready, PVC bound" — Postgres sub-app is parked, the actual data store lives in the `ruflo-data` PVC)*

- P3.T1.S2: ruvocal can read+write the RVF store *(was: "ruvocal can connect" against Mongo — that path is dead)*

### Task 2: ruflo pod-level health

- P3.T2.S1: Both containers Ready *(verified 2026-05-03 — `ruflo: ready=true restarts=0`, `ruflo-shell: ready=true restarts=0` after the #200 rollout)*

- P3.T2.S2: Confirm shareProcessNamespace works *(skipped — incompatible with agent-shell-base s6 init, removed in #196; see Deployment Notes deviation)*

- P3.T2.S3: Confirm `/workspace` is shared and writable from both containers

### Task 3: Authentik outpost provider assignment (manual)

- P3.T3.S1: Run the Django ORM command above to add the ruflo proxy provider to the embedded outpost. *(Done 2026-05-03 during Phase 3 validation.)*

- P3.T3.S2: Verify the outpost provider list includes `ruflo` via the verify command. *(Confirmed — `Ruflo (cluster)` in the embedded outpost provider list.)*

### Task 4: Web UI loads through Authentik SSO

- P3.T4.S1: Curl confirms Authentik intercepts unauthenticated requests

- P3.T4.S2: Browser SSO and confirm ruvocal UI loads

### Task 5: SSH connectivity from operator laptop

- P3.T5.S1: Service has the LB IP

- P3.T5.S2: SSH connects and lands in /home/agent

- P3.T5.S3: `claude` is on PATH (Layer-1 baked-in)

- P3.T5.S4: `cd /workspace` shows ruvocal's view

- P3.T5.S5: Mosh connects

- P3.T5.S6: tmux session persists across reattach

### Task 6: MOTD shows last-reconcile summary

- P3.T6.S1: Fresh login banner

### Task 7: Telegram alert path (induced failure)

- P3.T7.S1: Add a known-bad entry to the inventory

- P3.T7.S2: Run reconcile

- P3.T7.S3: Verify Telegram alert arrived in the configured chat with format `⚠ ruflo-shell: 1 install(s) failed on boot`.

- P3.T7.S4: Verify MOTD shows the failure on next SSH login.

- P3.T7.S5: Revert the inventory change — remove the bogus entry, commit, push, run reconcile again. MOTD should flip to the success line. No Telegram message expected on success.

### Task 8: Confirm zero direct frontier-LLM traffic from the pod

- P3.T8.S1: From inside the ruflo container, confirm there's no `ANTHROPIC_API_KEY`

- P3.T8.S2: Confirm LiteLLM is reachable

- P3.T8.S3: Confirm OpenRouter is reachable (without exposing the key in logs)

## Phase 4: Populate inventory and verify reconcile

### Task 1: Curate initial inventory

- P4.T1.S1: Edit `apps/ruflo/manifests/configmap-shell-inventory.yaml` to declare the operator's expected toolset. Suggested starting set (refine after a discovery week):

### Task 2: Run reconcile and verify

- P4.T2.S1: (with workaround — see P4.T2 Deployment Note) `ruflo-shell-reconcile` ran end-to-end (~7 min wallclock, dominated by cargo compile of ripgrep + eza). Final summary on the first run: `installed=7 already=0 removed=0 failed=2`. The 7 ✓: `mise install python@3.12 / node@20 / rust@stable`, `pipx install black / ruff`, `cargo install ripgrep / eza`. The 2 ✗: `npm i -g claude-flow@alpha` and `npm i -g @openai/codex` — both EACCES on `/usr/lib/node_modules/`, see Phase 4 Deployment Notes for the agent-shell-base bug. Workaround applied: `mise use --global node@20 rust@stable python@3.12` + `pip install pyyaml` (in mise's python; the inventory parser uses pyyaml), then npm-globals install cleanly.

- P4.T2.S2: Verify each manager reports the tool present — verified after the workaround above:

- P4.T2.S3: Confirm `claude-flow` CLI works

### Task 3: Persistence test across pod restart

- P4.T3.S1: Force a pod bounce — `kubectl -n ruflo-system rollout restart deploy/ruflo` + `rollout status` (60s, both containers Ready).

- P4.T3.S2: Reconnect and verify everything still present

- P4.T3.S3: Confirm ruvocal RVF state survived *(was: "Mongo state survived" — ruvocal at the pinned SHA writes to `/app/db/ruvocal.rvf.json`, persisted via the `ruflo-data` PVC added in Phase 3 / PR #200)* — *skipped here; the canonical proof was performed in PR #200* (recorded in the P3 Deployment Notes row dated 2026-05-03, "Test plan from #200 executed": fresh PVC pod logged `[RVF] No existing database … starting fresh`, post-restart pod logged `[RVF] Loaded 7 collections`, marker file `/app/db/.persist-test` survived). My Phase 4 bounce ran on the pre-#200 manifests where `/app/db` was overlay fs, so it did not exercise RVF persistence — re-running the same proof here would be redundant. Re-confirmed live during this PR's review that the persisted state is still intact:

### Task 4: Interactive install drift test

- P4.T4.S1: Manually install something not in the inventory — `cargo install fd-find` → `Installed package fd-find v10.4.2 (executable fd)` (~60s release build).

- P4.T4.S2: Confirm it persists across pod bounce — bounced via `rollout restart deploy/ruflo`, post-restart `cargo install --list` shows `fd-find v10.4.2` alongside eza + ripgrep, `fd --version` → `fd 10.4.2`. Layer-3 escape hatch works as specified.

- P4.T4.S3: Document the drift policy in this plan's *Deployment Notes * — added under P4.T4 (above): drift is intentional during discovery; promote to inventory ConfigMap when you want PV-migration survival, next-operator inheritance, or Telegram-alert coverage on the install. The inventory's `removed:` arrays are the explicit un-install path (vs. just deleting from the declared list, which leaves existing installs in place by design).

## Phase 5: Documentation

### Task 1: Update CLAUDE.md rules

- P5.T1.S1: Update `.claude/rules/frank-infrastructure.md` Frank Cluster Services table

- P5.T1.S2: Add gotchas to `.claude/rules/frank-gotchas.md` — only patterns that actually surfaced during Phases 1–4. Possible candidates (verify each was actually hit):

### Task 2: Write the building blog post

- P5.T2.S1: Run `/blog-post` to scaffold `blog/content/docs/building/<NN>-ruflo/index.md`. Choose `<NN>` so it follows the most recently published `building/` post in numeric order.

- P5.T2.S2: Write the post. Suggested narrative:

- P5.T2.S3: Update series index `blog/content/docs/building/00-overview/index.md` — add ruflo to the Series Index and Capability Map.

- P5.T2.S4: Update roadmap shortcode `blog/layouts/shortcodes/cluster-roadmap.html` — add the new layer entry.

### Task 3: Write the operating blog post

- P5.T3.S1: Run `/blog-post` to scaffold `blog/content/docs/operating/<NN>-ruflo/index.md`.

- P5.T3.S2: Write the post. Sections:

- P5.T3.S3: Update operating series index in `blog/content/docs/building/00-overview/index.md`.

### Task 4: Run `/update-readme`

### Task 5: Sync runbook

- P5.T5.S1: Run `/sync-runbook`. This plan introduces two manual-operation blocks (Infisical bootstrap in Phase 2 Task 1, Authentik outpost assignment in Phase 3 Task 3). The skill picks them up and merges into `docs/runbooks/manual-operations.yaml`.

### Task 6: Update plan status

- P5.T6.S1: ** Set ` Status:** Deployed` at the top of this plan.

## Phase 6: Post-Deploy Checklist
