schema_version: 2
phase:
  number: 4
  title: Docs + upstream PR + close
  tag: agentic
  depends_on:
  - 3
  tracking_issue: null
tasks:
- number: 1
  title: Update gotchas
  steps:
  - id: P4.T1.S1
    text: |-
      Update `docs/runbooks/frank-gotchas/paperclip-ruflo.md`: add a section documenting (a) `RvfGridFSBucket` is an incomplete GridFS shim — upload/download/find all needed parity fixes (with the incident date + the fix), (b) ruvocal is RVF-only at this revision (no Mongo backend to switch to; DATABASE_URL ignored), and (c) the per-model `supportsTools`/`forceTools` toggle requirement for MCP tools to load.
  - id: P4.T1.S2
    text: |-
      Add matching one-liners under the "Paperclip / Ruflo" section of `agents/rules/frank-gotchas.md` (hot file): the shim-parity fix and the supportsTools toggle.
- number: 2
  title: Upstream PR + close out
  steps:
  - id: P4.T2.S1
    text: |-
      File an upstream PR to ruvnet/ruflo with the `rvf.ts` GridFS-shim parity fix (and reference/include the still-pending `wasm://` urlSafety allow-line). Link the PR in the gotcha.
  - id: P4.T2.S2
    text: |-
      Retroactively update the ruflo layer's existing building/operating posts if the fix is narrative-worthy (gotcha + new operational note), then set the spec/plan `**Status:**` to Deployed. Run `/sync-runbook` only if any `# manual-operation` blocks were added (none expected).
state:
  steps:
    P4.T1.S1:
      state: ' '
      ticked_at: null
      note: null
    P4.T1.S2:
      state: ' '
      ticked_at: null
      note: null
    P4.T2.S1:
      state: ' '
      ticked_at: null
      note: null
    P4.T2.S2:
      state: ' '
      ticked_at: null
      note: null
  completion:
    at: null
    note: null
    observed_prs: []
