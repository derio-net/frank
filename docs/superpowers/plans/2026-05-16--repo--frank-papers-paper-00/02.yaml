schema_version: 2
phase:
  number: 2
  title: Gate validation
  tag: agentic
  depends_on: [1]
  tracking_issue: null
tasks:
- number: 1
  title: Run validate-dossier.py and resolve failures
  steps:
  - id: P2.T1.S1
    text: |-
      Run the dossier gate validator


      ```bash
      python scripts/validate-dossier.py docs/papers-dossiers/00-why-homelab-in-2026/dossier.md
      ```

      Expected: `DOSSIER GATE PASSED: ...`

      If it fails, fix the reported issues:
      - `vendors_in_scope: need ≥3` → add more vendors to the dossier
      - `primary_sources: need ≥5` → add more sources
      - `primary_sources: need ≥3 distinct type values` → diversify source types
      - `URL not reachable` → find an alternative URL or use a web archive link
      - `frank_artefacts: need ≥3` → add more artefacts
      - `frank_artefacts: need ≥2 distinct kind values` → add a different kind
      - `named_gaps: need ≥1` → add a gap
      - `counter-arguments_considered: need ≥1` → add a counter-argument

      Re-run until exit 0.
  - id: P2.T1.S2
    text: |-
      Human gate: author reviews named gaps and counter-arguments


      **This step requires human review.**

      The author (user) reads:
      1. `## Named gaps` — are the gaps honest and specific? Do they point at
         real holes in the literature rather than vague disclaimers?
      2. `## Counter-arguments considered` — is the key counter-argument
         ("cloud wins for production") addressed with the right framing
         ("Frank is a learning platform")?

      When satisfied, update dossier status:
      ```yaml
      # In docs/papers-dossiers/00-why-homelab-in-2026/dossier.md frontmatter:
      status: ready
      ```
  - id: P2.T1.S3
    text: |-
      Commit the validated dossier


      ```bash
      git add docs/papers-dossiers/00-why-homelab-in-2026/dossier.md
      git commit -m "docs(repo): Paper 00 research dossier — why homelab in 2026"
      ```
state:
  steps:
    P2.T1.S1:
      state: ' '
      ticked_at: null
      note: null
    P2.T1.S2:
      state: ' '
      ticked_at: null
      note: null
    P2.T1.S3:
      state: ' '
      ticked_at: null
      note: null
  completion:
    at: null
    note: null
    observed_prs: []
