# Journal: crowdsec-canary-alert-triage

<!-- fr:journal kind=repro scope=debug id=5882a308d013 created=2026-07-24T22:25:15 -->
### 5882a308d013 · repro · frank#599 two-part: Part1 telegram_direct routing already synced live; Part2 alert-agent stale/false triage

Part 1 (ops): #598 (merged 2026-06-21) routes crowdsec-canary-heartbeat-stale directly to Telegram via telegram_direct=true label + top-of-policy notification-policy route. Verified LIVE on Frank: notification-policy CM has the route (line 52), grafana-alerting-rules CM has telegram_direct count=3 incl the canary rule (ArgoCD synced #598). Grafana = victoria-metrics-grafana in monitoring ns (pod 3h24m old). Part 2 (bug): alert-agent (alert-agent ns, Frank, multi-agent-shell) produced confidently-FALSE triage 2026-06-21 — surfaced resolved #594 incident ('CrowdSec blind since 11:37Z yesterday, no bans since 09:55Z') as CURRENT while CrowdSec was actively banning (bans 13:30/13:33/13:47/13:59Z), and misattributed 4 Azure IPs (20.9.39.209 etc). Stale context bleeding into live triage.

<!-- fr:journal kind=finding scope=debug id=a71152846552 created=2026-07-24T22:26:02 state=fixed -->
### a71152846552 · finding [fixed] · Part 1 step 1: rollout-restarted victoria-metrics-grafana; provisioning reloaded clean

kubectl -n monitoring rollout restart deploy/victoria-metrics-grafana at 22:25:19Z (strategy=Recreate). New pod victoria-metrics-grafana-657dcd8647-vnq4p 3/3 Ready in ~13s. Grafana log: 'starting to provision alerting' -> 'finished to provision alerting' with NO error (dashboards 'default' path error is pre-existing cosmetic, unrelated). telegram_direct routing now definitively loaded from file provisioning at boot. Remaining: Test-2 end-to-end delivery proof.

<!-- fr:journal kind=finding scope=debug id=67df51690b8b created=2026-07-24T22:30:53 state=fixed -->
### 67df51690b8b · finding [fixed] · Part 1: telegram_direct routing PROVEN live+correct via Grafana API (read-only)

GET /api/v1/provisioning/policies on the freshly-restarted grafana pod returns the in-memory routing tree with route order: canary_watchdog -> canary(perma-mute) -> telegram_direct=true(Telegram-Willikins) -> blog-edge(AI Helper) -> gpu_timeshare -> health_bridge_only -> severity. Trace for the canary-heartbeat-stale alert {severity:critical, telegram_direct:true, folder:blog-edge}: not canary_watchdog, not canary(=>not muted), MATCHES telegram_direct=true => Telegram-Willikins continue:false => STOPS, never reaches blog-edge->AI-Helper. Contact point 'Telegram - Willikins' uid efi04e0201jb4f wired to $FRANK_C2_TELEGRAM_BOT_TOKEN/$FRANK_C2_TELEGRAM_CHAT_ID via ExternalSecret. Rule-fires link already proven per issue (it reached the agent pre-fix). Canary currently ALIVE+healthy (heartbeats every 5m, verdict=ok, filesource_delta>0, on hop-1). Residual literal step (suspend Hop canary 22m -> observe real Telegram msg) needs Hop credentials this isolated Frank pod lacks (Hop is standalone/non-Omni; talosconfig+kubeconfig gitignored, absent; 6443/50000 reachable but no client cert) AND is outward-facing to the operator's phone.

<!-- fr:journal kind=root-cause scope=debug id=84d930068d87 created=2026-07-24T22:42:10 -->
### 84d930068d87 · root-cause · alert-agent stale bleed: 3 autonomous wakes share persistent alert-agent-ops session; server only /clears on 12h idle

webhook.handle_alert, orchestration.run_surge, orchestration.run_digest ALL call bridge.session_send(..., session_id='alert-agent-ops'). The agent-session server (multi-agent-shell image /usr/local/lib/multi-agent-shell/agent-session, NOT in frank repo) maps session_id -> deterministic uuid5 -> claude --session-id (create-or-resume), keeping ONE long-lived claude process per id. It resets context via submit(id,'/clear') ONLY when (now-last_activity) > IDLE_RESET_S, default 12h (AGENT_SESSION_IDLE_RESET_S/STOA_IDLE_RESET_S). All 3 analytical wakes refresh alert-agent-ops's last_activity, so on active days wakes are <12h apart, idle-reset never fires, and a prior wake's narrative (the live-#594 CrowdSec-blind incident, most plausibly from the daily digest which reports crowdsec status) persists in the context window and bleeds into the later canary-heartbeat-stale triage -> re-emitted as current. The prompt's 'using ONLY the facts below' can't retract what's already in the window. Evidence: no reaper in server (grep), each live claude ~370-410MB RSS, only 2 tmux sessions now but many stale .last/.turn files -> unique-per-wake ids would OOM the 8Gi pod (ruled out). Note #598 already ROUTES the dead-man's-switch away from the agent (telegram_direct), neutralizing the exact observed alert; Part-2 fix hardens the general agent-triage path (other blog-edge alerts still hit the agent).

<!-- fr:journal kind=finding scope=debug id=53a52cf047ce created=2026-07-24T22:46:11 state=fixed -->
### 53a52cf047ce · finding [fixed] · Fix: per-stream + per-alertname agent sessions (kill shared alert-agent-ops); failing test pins isolation

Changed apps/alert-agent/handlers: webhook.handle_alert now drives a per-alertname session (alert-agent-webhook-<slug>, sanitized to the server's ^[A-Za-z0-9_-]{1,128} rule, prefix-only fallback on missing alertname); orchestration.run_surge->alert-agent-surge, run_digest->alert-agent-digest. This kills the shared alert-agent-ops session that let a prior wake's #594 narrative bleed in. Isolation properties: (1) surge/digest/alert streams mutually isolated -> a digest's crowdsec narrative can never surface in an alert triage (the most plausible #594 carrier); (2) different alert TYPES isolated -> one alert never bleeds into another; (3) same alert re-firing (repeat_interval 3m) reuses its own context (correct); (4) same alert >IDLE_RESET_S(12h) apart (new incident) gets the server's idle /clear -> a resolved incident can't resurface; (5) digest is guaranteed-fresh (24h cadence > 12h idle). Bounded (finite blog-edge alert rules) -> no unbounded session/process growth, so no 8Gi OOM (the reason unique-per-wake ids were rejected). Failing test tests/test_session_isolation.py (RED: all 3 shared alert-agent-ops) now GREEN; full handler suite 68 passed. Also refreshed the deployment.yaml memory-sizing comment. NOTE this is defence at the frank layer; the DURABLE root fix is a per-request context-reset flag in the agent-session server (multi-agent-shell/agent-images) so a one-shot analytical turn can force a clean context regardless of idle timing -> recommended follow-up (out of frank scope). #598 already routes the dead-man's-switch away from the agent, neutralizing the exact observed alert.
