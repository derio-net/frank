schema_version: 2
phase:
  number: 5
  title: '[manual] Live readiness, contract verification, content-factory writeback'
  tag: manual
  depends_on:
  - 2
  - 4
  tracking_issue: null
tasks:
- number: 1
  title: Provision + verify on gpu-1, then write back and close
  steps:
  - id: P5.T1.S1
    text: |
      [manual] MO-1 — After merge (CI builds the comfyui image), run the
      model-download Job on gpu-1; confirm LTX-2 NVFP8, Wan 2.2 14B Q8 GGUF,
      LTX-Video 0.9.x, Kokoro, Fish-Speech S2 are present on the comfyui-models
      PVC. Confirm/lock the weight source URLs in job-model-download.yaml.
  - id: P5.T1.S2
    text: |
      [manual] MO-2 — Activate ComfyUI via the GPU Switcher and tune the
      VRAM/offload flags under a real generation on the 16GB box; record the
      final flag values back into deployment.yaml (one-model-at-a-time fit).
  - id: P5.T1.S3
    text: |
      [manual] MO-3 — Assign the comfyui proxy provider to the Authentik embedded
      outpost via the Django ORM step (per frank-argocd.md); confirm
      comfyui.cluster.derio.net presents SSO.
  - id: P5.T1.S4
    text: |
      [manual] MO-4 — One-time `claude login` (subscription OAuth) inside the
      multi-agent-shell sidecar; confirm the credential lands on the agent-home
      PVC and the `stoa-script-claude` tmux session is live.
  - id: P5.T1.S5
    text: |
      [manual] MO-5 — Live contract verification (the Phase-2 S2 checks):
      (a) in-cluster POST comfyui.comfyui.svc:8188/prompt with a stoa graph →
      prompt_id + node_errors {}; GET /history/{prompt_id} → body keyed by
      prompt_id, body[prompt_id].status.completed true with outputs.<node>.gifs[];
      GET /view → bytes.
      (b) POST http://localhost:8765/session/send (n8n AGENT_SIDECAR_URL) →
      {status:ok, turn:N, payload:{…}}; a second send → turn:N+1; `tmux attach`
      to the session works.
  - id: P5.T1.S6
    text: |
      [manual] MO-6 — Second-repo writeback (agentic-stoa/content-factory,
      PRIVATE): record the live endpoints/route + this Frank PR URL in the
      Frank-request runbook; tick the Phase-2 steps in the content-factory pipeline
      plan; close content-factory#55. Technical only — no business context leaves
      into frank.
state:
  steps:
    P5.T1.S1:
      state: x
      ticked_at: '2026-06-15T14:28:33+00:00'
      note: 'MO-1: model-download Job ran on gpu-1; LTX-2 NVFP8+GGUF, Wan2.2 14B Q8
        (Hi/Lo MoE), LTX-Video 0.9.8, Kokoro, Fish-Speech 1.5 all present on PVC;
        URLs locked in job-model-download.yaml (HTTP 302 verified).'
    P5.T1.S2:
      state: x
      ticked_at: '2026-06-15T14:28:34+00:00'
      note: 'MO-2: activated via GPU Switcher; VRAM flags --reserve-vram 1.0 --cache-none
        recorded in deployment.yaml; one-model-at-a-time fit on 16GB confirmed.'
    P5.T1.S3:
      state: x
      ticked_at: '2026-06-15T14:28:36+00:00'
      note: 'MO-3: ComfyUI (cluster) proxy provider assigned to Authentik embedded
        outpost; comfyui.cluster.derio.net 302 -> Authentik SSO confirmed.'
    P5.T1.S4:
      state: x
      ticked_at: '2026-06-15T14:28:37+00:00'
      note: 'MO-4: claude login (subscription OAuth) done inside multi-agent-shell
        sidecar; credential on agent-home PVC; tmux session live.'
    P5.T1.S5:
      state: x
      ticked_at: '2026-06-15T14:28:39+00:00'
      note: 'MO-5: (a) ComfyUI submit->poll->fetch composes (object_info 736 classes
        incl. WanVideo/LTXV/Fish/gguf/Kokoro on -stoa3); (b) agent-session POST /session/send
        turn:2 then turn:3, payloads echoed, tmux attach works.'
    P5.T1.S6:
      state: x
      ticked_at: '2026-06-15T14:28:40+00:00'
      note: 'MO-6: content-factory PR #68 recorded live endpoints in frank-duanju-request.md,
        ticked Phase-2 steps, closed content-factory#55 (merged).'
  completion:
    at: '2026-06-15T14:28:42+00:00'
    note: All MO steps executed and verified live end-to-end; ComfyUI complete node
      set loads on -stoa3; agent-session round-trip verified; content-factory#55 closed.
    observed_prs: []
