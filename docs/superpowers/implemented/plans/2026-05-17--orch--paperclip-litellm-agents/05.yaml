schema_version: 2
phase:
  number: 5
  title: Hire LiteLLM-backed agents — end-to-end
  tag: manual
  depends_on:
  - 4
  tracking_issue: https://github.com/derio-net/frank/issues/275
tasks:
- number: 1
  title: Hire one opencode_local agent
  steps:
  - id: P5.T1.S1
    text: |-
      Two paths to hire — **prefer the UI**:

      - **UI (recommended):** http://192.168.55.212:3100 → board hire form. Sidesteps the board-mutation CSRF gotcha in `docs/runbooks/frank-gotchas/paperclip-ruflo.md` (which requires the `Origin` header + `%3D`-encoded cookie). The UI does that wiring for you.
      - **API via `paperclip-create-agent` skill:** Works from the dev box when you have `$PAPERCLIP_API_KEY` + `$PAPERCLIP_COMPANY_ID` set. Honor the runbook's CSRF + cookie-encoding rules; the skill does this correctly.

      Payload template (drop into UI form or skill `adapterConfig`):

      ```json
      {
        "name": "Local Coder",
        "role": "engineer",
        "title": "Local Engineer (opencode)",
        "icon": "wrench",
        "adapterType": "opencode_local",
        "adapterConfig": {
          "model": "<working-shape-from-P1.T1>"
        }
      }
      ```
  - id: P5.T1.S2
    text: |-
      Assign a trivial issue (e.g. "Reply 'ack' as a comment"). Watch the run in Paperclip's UI: transcript completes, no errors, comment posted.
- number: 2
  title: Hire one hermes_local agent
  steps:
  - id: P5.T2.S1
    text: |-
      Same UI-vs-skill choice as P5.T1.S1; same CSRF caveats on the skill path. Payload:

      ```json
      {
        "name": "Local Reasoner",
        "role": "engineer",
        "title": "Local Reasoner (Hermes)",
        "icon": "brain",
        "adapterType": "hermes_local",
        "adapterConfig": {
          "model": "qwen-think-14b",
          "hermesCommand": "/paperclip/agent-bin/bin/hermes",
          "toolsets": "terminal,file,web",
          "persistSession": true
        }
      }
      ```

      Notes:

      - `model` is the bare LiteLLM alias `qwen-think-14b`, NOT `litellm/qwen-think-14b` (that prefix is opencode-specific). Routing to ollama-cloud is set by `paperclip-hermes` ConfigMap's `model: "ollama-cloud/qwen-think-14b"` — the prefix on the **default** model pins ollama-cloud as the install's default provider, and that default applies even when the adapter passes `-m qwen-think-14b` (bare).
      - `hermesCommand` is set explicitly to the absolute path even though Phase 3 widens PATH to include `/paperclip/agent-bin/bin`. Belt-and-braces against future PATH drift.
      - Do NOT set `provider: "ollama-cloud"` — `hermes_local`'s `VALID_PROVIDERS` whitelist drops it silently (the whitelist is stale vs. the upstream hermes CLI but the field is a no-op here anyway). Do NOT type anything into the UI's `extraArgs` text input — the schema-form stores it as a single-element array (one argv token with embedded space), which hermes argparse rejects with `unrecognized arguments`. Neither of these matter because we don't actually need any extra CLI args — see `docs/runbooks/frank-gotchas/paperclip-ruflo.md#litellm-backed-agents` and the Phase 5 Deployment Deviation entry in `_prose.md`.
  - id: P5.T2.S2
    text: |-
      Assign a second trivial issue. Watch the transcript flow back as structured tool cards (hermes-paperclip-adapter parses Hermes's stdout into typed entries).
- number: 3
  title: Cross-verify on LiteLLM
  steps:
  - id: P5.T3.S1
    text: |-
      Confirm both calls landed on LiteLLM and routed to Ollama (not OpenRouter).

      ```bash
      kubectl -n litellm logs -l app.kubernetes.io/name=litellm --tail=200 \
        | grep -E 'ollama/(qwen2.5-coder|qwen3:14b|mistral-small)' | tail -10
      ```

      Expected: both agents' calls appear with model=`qwen-coder-14b` (opencode) and `qwen-think-14b` (hermes).
  - id: P5.T3.S2
    text: |-
      Pod-restart resilience: restart the paperclip pod (`kubectl rollout restart`), wait for it to come back, and rerun a heartbeat on the hermes agent. Confirm it resumes its session via `--resume` (no fresh-start banner).
state:
  steps:
    P5.T1.S1:
      state: x
      ticked_at: '2026-05-17T19:56:02+00:00'
      note: Hired via UI; transcript completed successfully (operator-verified)
    P5.T1.S2:
      state: x
      ticked_at: '2026-05-17T19:56:04+00:00'
      note: Trivial issue ack posted via opencode_local agent (operator-verified)
    P5.T2.S1:
      state: x
      ticked_at: '2026-05-19T14:06:11+00:00'
      note: 'Hired Media Pipeline Engineer (UUID 30f6c830-...) as hermes_local with
        model qwen-think-14b + hermesCommand /paperclip/agent-bin/bin/hermes; UI Test
        passed after revert PR #297 (cosmetic ''No LLM API keys'' warning expected
        — adapter test-env hard-coded to look for cloud keys, our deployment uses
        OLLAMA_API_KEY)'
    P5.T2.S2:
      state: x
      ticked_at: '2026-05-19T14:06:13+00:00'
      note: First heartbeat 7745ce28 exited 0 in 32s; hermes ran with model=qwen-think-14b
        provider=ollama-cloud via config.yaml default-provider inference; LiteLLM
        logged POST /v1/chat/completions 200 OK from paperclip pod IP. Subsequent
        heartbeats blocked by upstream session-ID truncation bug — see derio-net/paperclip#1
        + P5.T3.S2 below
    P5.T3.S1:
      state: x
      ticked_at: '2026-05-19T14:06:15+00:00'
      note: LiteLLM access log on pod litellm-5d7466bfd5-2psn6 shows 'POST /v1/chat/completions
        200 OK' from 10.244.10.177 (paperclip pod IP). Opencode_local already verified
        earlier with model litellm/qwen36-a3b-nothin → ollama/qwen3.6:35b-a3b. Both
        adapter types confirmed routed through LiteLLM to Ollama (not OpenRouter)
    P5.T3.S2:
      state: '-'
      ticked_at: '2026-05-19T14:06:17+00:00'
      note: 'Blocked by upstream bug: hermes-paperclip-adapter@0.2.0 truncates session
        ID to 16 chars (slice(0,16) in execute.js:385), compounded by paperclipai/paperclip''s
        heartbeat.ts giving explicitDisplayId priority over runtimeSessionParams.sessionId.
        Net effect: any hermes_local agent fails on its 2nd heartbeat onward. Pod-restart
        resume is a special case of multi-wake and is broken by the same code path.
        Issue: derio-net/paperclip#1 (https://github.com/derio-net/paperclip/issues/1).
        Workaround documented in runbook (set adapterConfig.persistSession=false on
        hires that need to survive multi-wake). Will revisit P5.T3.S2 once upstream
        patch lands and a paperclip image bump is available'
  completion:
    at: '2026-05-19T14:09:01+00:00'
    note: Hires + first-heartbeat e2e routing verified for both adapter types (opencode_local
      → LiteLLM/ollama, hermes_local → LiteLLM/ollama). Pod-restart resume (P5.T3.S2)
      deliberately skipped — blocked by upstream hermes-paperclip-adapter session-ID
      truncation bug + paperclip displayId-priority chain; tracked at derio-net/paperclip#1,
      workaround documented in runbook + frank-gotchas. Will re-test P5.T3.S2 once
      upstream patch lands and a paperclip image bump becomes available.
    observed_prs: []
