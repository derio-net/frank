## Enable CDI device discovery in containerd for Intel GPU DRA
## Applied to mini-2 only
## Does NOT require a node reboot — containerd restarts automatically
metadata:
    namespace: default
    type: ConfigPatches.omni.sidero.dev
    id: 303-mini2-cdi-containerd
    labels:
        omni.sidero.dev/cluster: frank
        omni.sidero.dev/cluster-machine: 6ea7c1c6-6ba6-b59d-c77a-88aedd676447
spec:
    data: |
        machine:
            files:
                - path: /etc/cri/conf.d/20-customization.part
                  op: create
                  content: |
                      [plugins."io.containerd.cri.v1.runtime"]
                        cdi_spec_dirs = ["/var/cdi/static", "/var/cdi/dynamic"]
