{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "case \"$CLAUDE_FILE_PATH\" in *.env_devops|*.sops.yaml|*/.talos/*) echo 'BLOCK: This is a sensitive file (.env_devops, .sops.yaml, or .talos/ config). Please confirm with the user before editing.' >&2; exit 2;; *) exit 0;; esac"
          }
        ]
      }
    ]
  }
}
