apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vibekanban-db-data
  namespace: secure-agent-pod
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
