apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: agent-home
  namespace: secure-agent-pod
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 50Gi
