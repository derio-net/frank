# RBAC for the staging-gate Pipeline. RUNTIME-verified in the manual phase (P7) — the
# vCluster-access mechanism (the `vcluster-staging-kubeconfig` Secret) is created out-of-band
# alongside the ArgoCD cluster Secret.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: staging-gate
  namespace: tekton-pipelines
---
# Read the <app>-staging ArgoCD Application status (await-sync task polls Synced+Healthy).
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: staging-gate-argocd-read
  namespace: argocd
rules:
  - apiGroups: ["argoproj.io"]
    resources: ["applications"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: staging-gate-argocd-read
  namespace: argocd
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: staging-gate-argocd-read
subjects:
  - kind: ServiceAccount
    name: staging-gate
    namespace: tekton-pipelines
---
# Read the gate's secrets in tekton-pipelines: frank-gitops-push (the App token every
# clone/push step authenticates with over HTTPS — see tasks.yaml's git-plumbing note) and the
# vCluster kubeconfig (run-smoke/reset target the staging vCluster via --kubeconfig).
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: staging-gate-secrets-read
  namespace: tekton-pipelines
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    resourceNames: ["frank-gitops-push", "vcluster-staging-kubeconfig"]
    verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: staging-gate-secrets-read
  namespace: tekton-pipelines
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: staging-gate-secrets-read
subjects:
  - kind: ServiceAccount
    name: staging-gate
    namespace: tekton-pipelines
