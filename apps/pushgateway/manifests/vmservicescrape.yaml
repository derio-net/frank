apiVersion: operator.victoriametrics.com/v1beta1
kind: VMServiceScrape
metadata:
  name: pushgateway
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: pushgateway
  endpoints:
    - port: http
      honorLabels: true
