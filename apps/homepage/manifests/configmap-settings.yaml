apiVersion: v1
kind: ConfigMap
metadata:
  name: homepage-settings
  namespace: homepage
data:
  settings.yaml: |
    title: Frank Cluster
    background:
      opacity: 50
    theme: dark
    color: zinc
    headerStyle: clean
    layout:
      Infrastructure:
        style: row
        columns: 3
      Development:
        style: row
        columns: 3
