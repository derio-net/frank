"""frank-facts — deterministic observability fact builders (stdlib-only).

Ported from the retired ai-alert-helper's facts.py/surge.py. No third-party
deps (httpx/respx dropped) so it runs as a mounted script on the
multi-agent-shell image's system python3, like fetch-text.
"""
