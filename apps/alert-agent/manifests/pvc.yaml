apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: alert-agent-home
  namespace: alert-agent
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 5Gi   # ~/.claude login state + agent-session turn state + surge state
